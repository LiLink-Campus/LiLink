#!/usr/bin/env bash
# Retired: this entry point relied on production defaults or shared VPS services.
echo "Retired load test. Use the guarded scripts/release workflow; production targets are refused." >&2
exit 1
