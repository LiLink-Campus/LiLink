# Dependencies
node_modules/
apps/*/node_modules/
.npm-cache/
.codex-local/

# Build outputs
.next/
apps/*/.next/
dist/
apps/*/dist/
coverage/
apps/*/coverage/
out/
build/

# Runtime
logs/
*.log
*.pid

# Environment
.env
.env.*
!.env.example
apps/*/.env
apps/*/.env.*
!apps/*/.env.example

# Database
*.db
*.db-journal
apps/api/prisma/*.db

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
AGENTS.override.md
*.swp
.eslintcache

# Docker
postgres-data/
mailpit-data/

# Postgres dumps (pg_dump / scripts/backup-postgres.sh).
# No trailing slash so it also ignores the symlink form (backups -> /mnt/data/backups).
backups

# TypeScript
*.tsbuildinfo
next-env.d.ts
apps/api/src/generated/prisma/

# Misc
.vercel/
.turbo/
tmp/
*.pem

# Anonymous user DB exports (see apps/api/scripts/export-anonymous-user-data.mjs).
/anonymous-user-export*.json

# Load-test scratch files (generated by loadtest/seed-test-users.sh).
loadtest/.accounts.txt
loadtest/.staging-secrets/

# Standalone poster render subproject (independent npm workspace, ships its
# own node_modules; not part of the main app build)
marketing/
