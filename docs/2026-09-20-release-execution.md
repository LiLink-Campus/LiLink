# 秋季问卷发布执行记录

用户已授权：补完功能 → 独立升级与恢复演练 → 用户流程和压测 → 全部通过后部署。生产尚未变更，发布门槛未全部通过。

## 当前结论（2026-09-21 更新）

生产 `lilink.top` / `www.lilink.top`、API 与数据库仍为原版本。Vercel 登录已恢复，隔离 Preview 已接通；下面旧段落保留为历史过程，不代表当前阻塞状态。

### 当前候选与未通过项（05:00 新加坡时间）

前后端候选均为 `11293b6ea6d73fc6657aada02cd9514b60c07c44`，Vercel Preview 为 `dpl_GfFe5APAZaLpq1vgSDa4BWEcM6Mq`。该 SHA 的 push CI `35535457746`、PR CI `35535460253`、Browser E2E `35535460329`、Storybook `35535460254` 全部成功。Mac API 使用生产 Dockerfile，2 CPU / 3,584 MiB，60 条数据库连接，正常后台和 Mailpit 发送开启；此连接池设置没有应用到生产。

| 最新验证 | 实际结果 | 判定 |
| --- | --- | --- |
| 热态读取 33 req/s、120 秒 | 3,961/3,961 业务成功，bootstrap P95 356 ms | 通过 |
| 热态读取 67 req/s、120 秒 | 8,041/8,041 业务成功，bootstrap P95 526 ms | 通过 |
| 热态读取 100 req/s、120 秒 | 12,000/12,000 业务成功，bootstrap P95 1,556 ms / P99 2,293 ms | 超过 800 / 2,000 ms，失败；未继续 200 req/s |
| 混合读写约 50 API req/s、300 秒 | 15,002 次 API 尝试、14,996 次成功；11,534/11,540 次业务成功，零丢弃/429 | bootstrap P95 1,220 ms、写入 P95 6,270 ms，失败；未继续 100 req/s |
| 撮合期间 33 req/s | 5,138 次尝试、5,112 次业务成功、26 次失败，约 157 秒中止 | 失败；不是 30 分钟通过 |
| Preview 桌面与移动流程 | Chromium 148（1440×900）、WebKit 26.4（390×844）均完成保存、刷新、报名/取消、开信和已读保持，合成资料已还原 | 通过；登录前的未认证 `/auth/me` 401 属预期，不称零 HTTP 错误 |
| Neon 自然休眠后唤醒 | 首次登录 1,532 ms，读取 996 ms，原值资料保存 2,333 ms，回读 306 ms；热态再登录 250 ms | 冷态功能通过；单次样本不是 P95，也不是热态写入门槛 |

三档热态读取均无丢弃任务或 429，业务完成量不能替代延迟门槛。500 / 1,000 人撮合分别准备 7,672 / 14,388 ms、生成 250 / 500 对、500 / 1,000 快照；Mailpit 分别实收 500 / 1,000 封并逐收件人核对，重复 tick 不重发。揭晓含快照分别 19,064 / 31,686 ms，揭晓至队列排空 93,762 / 169,891 ms。1,000 人的揭晓事务 20,061 ms、快照 10,567 ms；去重重建修复消除了该轮 API 事务错误，但部分正常读取仍超时，禁止进入 2,000 人档。

对应证据：`mac-load-read-*-11293b6-pool60-hot-normal.json`、`matching-under-read-11293b6.json`、`mac-load-read-33-1800-1s-11293b6-pool60-normal-matching-soak.json`、`preview-browser-11293b6-1789936243086/report.json`、`cold-api-11293b6.json`。冷态测试前以 Neon 元数据确认 IDLE，期间未用 SQL 采样保持唤醒。

冷态测试之后恢复 API 的命令虽返回成功，启动迁移随后因 P1001 数据库连接失败退出。独立 SSR 运行 `35536610409` 的 31 页因此全部业务断言失败；HTTP 200 中含流式渲染错误，不能算作有效容量测试。已保留失败、重新启动并核验真实 health / 业务读取，同时补上恢复脚本的就绪等待、测试身份端点的上游健康校验，以及入口 502 计数。SSR 断言要求真实 h1 与当前用户，并拒绝流式错误记录，修复后冒烟 11/11 页通过。

独立 GitHub SSR 复测 `35537167472`：60 人/分钟冒烟 31/31 页正确；500 人/分钟、120 秒完成 1,001 次尝试、996 次业务成功，5 次连接失败，P95 1,773 ms，零丢弃和 429。其脚本门槛通过，但不是全成功；四个 API 入口各 997 次（均 200），与计划请求量的 99.5% 门槛相符，与全部浏览器尝试数不完全一致。1,000 人/分钟档在约 56 秒达到错误停止阈值：651 次完成尝试、625 次业务成功、26 次失败、80 次丢弃、178 次仍在执行的迭代被中断，HTML P95 11,949 ms；没有执行后续直接匹配页档位。窗口内四个 API 分别到达 823/824 次，包含被中断页面的服务端请求，不能当作成功页面数。证据：`network-probe-35537167472/reconciled.json` 和原始 k6 日志。45 次资源采样均存活且无 OOM，CPU 最高 34.57%、内存最高 9.06%。

该 SSR 失败窗口的 200 条关联日志样本中，公开问卷在 API 网关的 P95 为 6 ms，但前端等待总时间 P95 为 7,719 ms；bootstrap 分别为 955 / 6,698 ms。日志只是部分样本，说明链路中还有网关以外的等待，不能据此判定唯一根因或推算新加坡容量。独立发压 secrets 已删除。SSR 脚本归入 `scripts/release/load-ssr.js`，保留目标、SHA、真实用户页面、错误率、完成量与丢弃迭代检查。

为区分环境差异，仅对同一合成数据库做两地各 6 次只读诊断：Mac 小查询 87–90 ms，新加坡 3.4–6.9 ms；512 KiB 合成内容传输分别 197–409 ms 与 11.8–18.6 ms。新加坡诊断使用现有服务器的短暂只读进程，没有连接生产数据库或对生产服务加压。这只能证明链路差异，不能证明全部失败由网络造成，也不是生产容量验证。证据：`db-path-comparison-11293b6.json`。

混合档以每分钟 2,308 个业务操作产生约 50 API req/s，70% 读、20% 问卷保存并回读、10% 报名并回读。网关问卷保存 P95 3,263 ms、报名 P95 7,273 ms；5 次 API 500 对应数据库连接中断，另有一次发压端超时但服务器随后完成报名。不能自动重放这类已开始的写入。首轮 2,000 名合成用户全部完成过正确业务；之后的 6 次失败仍计入总结果。证据：`mac-load-mixed-2308-300-1m-11293b6-pool60-normal-mixed50.json`，并保留同期数据库等待/锁与资源采样。

尚未通过：100 req/s、混合写入、揭晓并发读取、30 分钟持续运行和完整 SSR 档位。用户已收到独立新加坡临时主机的具体选择（2 核 / 约 4 GiB、总费用不超过 ¥20、演练后释放），尚未授权新增付费资源；继续保留 Mac 发压方案。生产受控验收还需要指定允许修改的现有测试账号。上述门槛通过前，PR 保持 Draft，不执行生产迁移或流量切换。

本轮实际运行的前后端仍为 `11293b6`；`bc1d119` 只补演练入口、SSR 脚本、夹具发布顺序和 CI 覆盖，不冒称已作为新 API 镜像压测。5 项本地发布防护测试与 k6 初始化检查通过。收尾已撤销测试别名临时访问权限、删除全部 5 个本任务 GitHub secrets、停止任务专属 API/Mailpit/Caddy/代理/tunnel 与维护页预览；保留数据库分支、加密备份和报告。Neon 测试节点已请求挂起，不继续空转。重新开始需要按确切候选 SHA 配齐前后端与临时访问配置。05:02 新加坡时间只读复核两个正式域名仍指向 `dpl_4Q4qwqEQQ3BGwCEdWkSPwzAujTCg`；生产 API 仍运行原镜像 `sha256:b11d39608396be5f58db793a24ca48c545d29521729997ffe79d915e5330f15c`。

### 网关一致性与维护预热（04:10 新加坡时间）

`c9fdc9b` 的两次 CI、Browser E2E、Storybook 全部成功。公开接口 10/25/50 req/s 各 120 秒分别完成 1,200 / 3,000 / 6,001 次正确响应，零丢弃和 429；学校、用户、配对及题目数量断言通过，入口日志计数一致。

真实生产 Caddy 2.11.4 使用 `/home/admin/lilink/Caddyfile`，已有 `encode zstd gzip`。测试直通代理缺失压缩，问卷响应 12,921 字节；补齐同版本网关后 gzip 为 3,282 字节，解压正文完全一致。独立 GitHub 发压端、相同 `c9fdc9b` 前后端的 500 人/分钟、60 秒 SSR 从完整 HTML P95 7,077 ms 降至 1,503 ms：501/501 页业务正确，零丢弃、零 429，P99 2,409 ms。证据：`network-probe-35532870642/`（失败）、`network-probe-35533285969/`（通过）、`caddy-parity-check.json`。关联日志的部分样本也显示问卷 API 网关 P95 约 8 ms、前端总等待约 237 ms；部分样本不能代替完整 k6 统计。Caddy 运行在 Mac，API 为受限 Linux arm64 容器；CPU 架构、网关资源和网络仍与生产不同。

80 连接池的冷启动 100 req/s 检查完成 2,937 次业务但丢弃 63 次；后续低负载也超出延迟门槛。人为删除 3,500 条快照再以 67 req/s 读取，触发大量同步补建，约 31 秒因超时率中止。已从限定合成数据库的备份恢复 3,500 条原快照，逐项 JSON 一致、邮件数量不变。上述失败均保留，不能作为热态容量通过证据。

新增镜像内 `scripts/prewarm-dashboard-snapshots.mjs`，在 API 停止、维护开启且后台/邮件关闭时补齐已揭晓历史；显式核验 SHA、数据库主机和名称，默认只读。真实 Neon 演练约 54.8 秒达到 8 个已揭晓周期、9,500/9,500 快照覆盖，匹配、联系方式授权和邮件记录摘要未变化；刻意移除的 3,500 条快照恢复内容一致。另验证有快照但无参与记录的历史仍保留。第一次该边界测试因测试脚本转换 PostgreSQL 无时区时间戳而失败，已修复合成夹具、改用数据库 JSON 原值重跑通过；不是预热 CLI 修改了保留记录。证据：`prewarm-cli-c9fdc9b.json`、`snapshot-rehearsal-c9fdc9b-prewarm.json`、`prewarm-retained-v2-c9fdc9b.json`。

撮合并发演练尚未通过：原夹具把历次已揭晓周期改为 DRAFT，引发额外补建，已修复为保留 REVEALED；测试收件箱默认只保留 500 封，无法证明 1,000 封实收，现提高至 20,000 并保留逐收件人断言。失败轮的 500 人业务完成、实收 500 封；1,000 人数据库 SENT 为 1,000，收件箱只见 500，按失败处理。原揭晓状态已恢复。更高热态负载、修复后的撮合并发、混合写入与 30 分钟持续运行仍待完成。

Mac 环境现在要求 `RELEASE_CADDY_BINARY` 指向经官方校验和核验的 2.11.4 可执行文件；`local-rehearsal.mjs start` 启动压缩网关并核验候选 SHA，`stop` 只清理本任务记录的进程与容器。临时 GitHub 发压 secrets 已删除。生产尚未操作。

### 揭晓并发读取修复（04:25 新加坡时间）

`95ed2aa` 已完成 Caddy 集成启动，10 req/s、60 秒的 601 次业务全部正确，bootstrap P95 513 ms。随后 33 req/s 与撮合并发时，1,000 人揭晓暴露真实缺陷：等待批量快照的读取又排队启动单人重建，造成事务等待与超时。该轮在约 152 秒后触发停止阈值，4,979 次完成尝试中 4,910 次成功、69 次失败、零丢弃；不是 30 分钟持续运行通过。停止撮合驱动后让已入队邮件排空，500/1,000 人分别生成 250/500 对、500/1,000 快照，共实收 1,500 封且每个 outbox 仅一次发送；重复 tick 没有重发。未继续 2,000 人档。两档揭晓事务分别约 10.9/19.2 秒，快照约 6.7/10.4 秒，均须单列。

候选修复让懒补建在等候批量重建后重新确认快照是否仍缺失，避免再次开启写事务；显式刷新仍保留原有锁与重建行为。59 项单测和 16 项真实 PostgreSQL 生命周期回归通过，含 100 个并发等待者、批量失败后的补建，以及注销/屏蔽/联系方式并发。PostgreSQL 证据：`artifacts/e2e/4683c1a5872e/`。新候选容量尚待复验。

演练驱动现在支持保持普通后台任务运行，遇到调度器与手动 tick 竞争时必须核验数据库终态；正常读压测失败则写入本任务停止标记，当前档邮件核验后禁止进入下一档。这样持续运行测试可包含真实后台与邮件轮询，不能把暂停后台的测试称作完整运行态验收。

### 较早的候选记录

`b2d8bab` 的两次 CI、Browser E2E、Storybook 全部成功。40 连接热态首页每分钟 1,000 人、120 秒完成 2,001 次业务 / 8,004 次 API，全部成功、零 429/丢弃，bootstrap P95 630 ms；四个接口入口日志各 2,001 次，计数一致。33/67 API req/s 各 120 秒也通过。100 req/s 在 40 连接下 bootstrap P95 1,125 ms，60 连接下全部 12,000 次请求成功但 P95 966 ms，仍未通过 800 ms 门槛，已停止升级负载。连接预算只在隔离环境调整，生产未调整。证据为 `mac-load-*-b2d8bab-pool40-*`、`mac-load-*-b2d8bab-pool60-*`、`db-*.json` 和 `resources-*.json`。

`413e9e6` 复用同次 SQL 中的历史参与状态，只有确实存在参与记录且缺快照时才补建；已有历史快照即使缺参与记录仍保留读取。44 项账号单测和 20 项 PostgreSQL 问卷、轮次、揭晓及隐私回归通过（`artifacts/e2e/188858006373/`）；两次 CI、Browser E2E、Storybook 均成功。前一轮命令误写了生命周期测试文件名，已有 5 项通过但整轮失败，未算作通过证据；已用正确路径重新执行。

`413e9e6` / 60 连接的 100 req/s、120 秒完成 12,001 次业务请求，全部成功、零 429/丢弃；bootstrap P95 906 ms，仍未通过 800 ms。独立 GitHub 发压端验证真实 Vercel 首页 500 人/分钟、60 秒完成 501 页，页面业务断言全部成功、零丢弃/429，但完整 HTML P95 8,803 ms，超过 3 秒。API 入口四条路由各 501 次全部 200，bootstrap P95 1,195 ms。不能把端点耗时或流式 HTTP 200 当作页面完成。证据：`network-probe-35531616029/`。临时 GitHub 发压凭据已删除。

真实限流配置下，同一用户前 1,000 次请求成功，第 1,001 次返回 429；同一 IP 的另一用户仍为 200。移动 Chromium 保持在 `/dashboard` 并出现可重试错误，没有跳回登录页。公开接口压测另发现合成学校缺邮箱域名，学校列表为空导致断言失败；已补齐 seed 数据，原失败记录保留，修复后的完整矩阵待跑。

候选加入默认关闭的 `SERVER_API_TIMING_LOG_ENABLED`，仅在发布分支 Preview 开启，用随机请求标识关联前端 API 等待时间与测试网关处理时间；不记录 Cookie、查询参数或响应正文。3 项回归验证空响应、429 与日志隐私。Vercel 日志已确认慢请求为热函数调用，尚不能归因于冷启动或生产服务器容量。

已准备静态维护产物 `dpl_3hGw4J4bLqbB5GE8Rztw5Biq74cC`，内置浏览器验证远端 503、`Retry-After: 60`、`Cache-Control: no-store`；本地内置浏览器桌面/移动以及 WebKit 390/480/481/1440 宽度的布局和刷新通过。`vercel deploy --prod --skip-domain` 仍自动分配项目默认 `vercel.app` 别名，已立即恢复到原生产 deployment 并核验；两个正式域名从未改绑。不能把此参数理解为不产生任何别名副作用。产物没有访问 API、数据库、SMTP 或分析服务，尚未用于正式维护。

完整压力矩阵、撮合与正常读取并发、邮件实收与排空、Vercel SSR 容量、30 分钟持续运行以及正式停写窗口验收仍须完成。保持 Draft，未宣告上线。

## 已实现

- 旧答卷原样归档，独立新版空白状态；保存必须携带当前版本，报名和撮合验证完整当前问卷。
- 匹配参与记录冻结介绍、性别和性别偏好，保留来源；权限继续动态判断。
- 保存用户填写的 VIP 偏好，匹配时应用实际权益；最低颜值只接受连续下限区间。
- 维护、后台任务、邮件发送独立开关；受控 Cookie 只放行维护入口，仍须账号鉴权。
- 已签名 `/me/*` 会话按用户限流，其他入口保留 IP 限流；服务异常不再伪装为失效登录。
- 旧压测入口拒绝执行，新数据库演练入口校验独立项目、分支、计算节点和直连地址。

## 已取得证据

| 验证 | 结果 |
| --- | --- |
| API 单元测试 | `128e283` 远端 65 套、589 项通过，含维护/邮件/后台暂停、签名限流及求解器一致性 |
| PostgreSQL API E2E | `128e283` 远端 21 套、110 项通过，含 500 人批量撮合、冻结资料、写入冲突原子回滚及维护/邮件恢复 |
| 浏览器功能 E2E | Chromium、移动 Chromium、WebKit、移动 WebKit 共 84 项通过 |
| 独立 Neon 升级 | `br-curly-feather-aoc2i9ik`，35 → 44 条迁移，无失败；重复 deploy 无待执行项 |
| 原值核验 | 252 条完整归档与原答卷聚合摘要相同，252 条新版空白，654 条历史参与资料冻结，24 题 |
| 业务关联 | 276 个账号与资料、202 条联系方式、390 张券、8 条核销、8 条举报、327 对匹配与 654 条参与记录的数量及原字段摘要一致 |
| 加密恢复 | 独立分支已从加密逻辑备份恢复；全部聚合摘要和 35 条原迁移与备份前一致 |

原始证据在 Git 忽略目录 `artifacts/questionnaire-release-20260920/`。浏览器报告 `artifacts/e2e/740f2bf6475a/`；API E2E `artifacts/e2e/469d14a7744a/`，新增批量写入回归 `artifacts/e2e/289cf136505c/`。最新完整 PostgreSQL E2E 本地记录为 `artifacts/e2e/479342cb329e/`。不提交用户明细、连接串、密钥或备份。

## 正在验证与停止条件

独立 Linux 生产镜像与真实限流压测尚未通过。压测库是新建合成数据项目 `patient-meadow-65557384` / `br-muddy-poetry-azax6deb`，0.25–2 CU，计算配额上限 28,800 CU 秒；独立 API 限制 2 CPU / 3.5 GiB，单次运行最多 130 分钟。GitHub runner 的机房与生产不同，网络时延需单列，不能冒充同区域生产容量。临时 Cloudflare Tunnel 与 DNS、GitHub 演练 secrets 在任务结束后清理。

Vercel 当前内置浏览器仍待用户登录。后续需精确 SHA 的全部 CI、隔离前后端链路、内置浏览器目标状态检查、压测实际完成量、生产停写备份与 staged production 验证。任何数据、权限或容量门槛失败均不开放生产。

现场通过 ECS 元数据核实生产服务器区域为 `ap-southeast-1`（新加坡），与 Neon 同区域。用户提出使用 Mac 模拟压测，因此继续用 Mac 发压及独立 Linux arm64 容器，不创建付费测试机。容器 CPU/内存设为生产近似限制，架构、CPU 单核性能和网络差异必须单列。

`128e283` 的两次 CI、Browser E2E、Storybook 均已通过。独立 Linux + Neon 三档真实业务验证也全部完成：500 / 1,000 / 2,000 人分别生成 250 / 500 / 1,000 对，揭晓后有 500 / 1,000 / 2,000 条完整快照。准备耗时约 12.5 / 18.3 / 104.5 秒，揭晓含快照约 12.1 / 13.4 / 17.4 秒；健康请求最大 50 / 10 / 40 毫秒。2,000 人准备未满足 60 秒目标，不能将工作流成功等同于容量通过。后续脚本把这一时间目标也变成失败断言。

Mac 的 2 CPU / 3.5 GiB Linux arm64 容器，纯计算 2,000 人无历史约 12.6 秒、RSS 峰值 1,135 MiB；带历史排除和连续未匹配优先级约 37.6 秒、RSS 峰值 1,490 MiB。Mac 直接连接合成 Neon 多次超时/TLS EOF；临时对单个测试域名改用另一现有代理路线仍失败，已撤销并核实规则消失，没有持久修改代理配置。本地任务容器与网络已清理，生产没有变化。

Mac 发压到独立 Linux 的首页 API 链路冒烟：30 秒、每秒 1 位用户，实际完成 31 位用户 / 124 次 API 请求，全部业务断言成功，0 次意外 429，0 个丢弃迭代。读取 P95 3,024 ms / P99 4,324 ms，四接口整组 P95 3,985 ms，延迟门槛失败。尚未通过 33/67/100 req/s、混合写入、持续运行及 Vercel SSR 的容量验收。证据为 `home-smoke-128e283.json`、`linux-128e283-run.log` 和 `container-*-128e283.jsonl`。

后续候选进一步复用求解器的字节标记缓冲区、直接遍历邻接表，保持权重与遍历顺序；400 组新旧完整配对结果一致。压测代理补齐多字节错误凭据的 403 验证、请求时间戳和分接口延迟，其远端结果见下方最新状态。

恢复边界以第一笔新写入计算，包含维护期验收；之后保留新写入并优先前向修复。生产接受新写入前才适用停写备份 RPO=0。

## 压测发现与修复（候选仍在验证）

- 第一轮独立 Linux 的 500 人撮合计算选出了 250 对，但逐对写入触发默认 5 秒事务超时，HTTP 201 的 `preparedCycleIds` 为空。已改为两次批量插入，并保留事务内的准备状态锁和唯一约束；事务有 30 秒上限。
- 旧计算阻塞 API 事件循环约 2.9 秒。候选把同一套精确评分和 Blossom 求解移入单独 worker，最多一个计算运行、三个排队，120 秒超时，worker 堆上限 1.5 GiB。故障会返回错误并进入既有准备恢复流程。规则单测与真实线程结果一致性、故障后下一任务执行均通过。
- 本地完整计算（24 题、稠密候选）500 / 1,000 / 2,000 人分别约 1.0 / 4.6 / 28.5 秒，进程峰值 RSS 269 / 635 / 1,413 MiB，主线程最大事件循环延迟约 22 毫秒。此项不是数据库、Vercel 或生产吞吐证据。
- 独立 Linux 第二次低负载检查 2 次业务请求/秒、20 秒共 41 次迭代，39 次成功、2 次传输 EOF；P95 约 6,050 毫秒、无丢弃迭代、无 429。未通过发布阈值。GitHub runner 与新加坡 Neon 的跨区延迟及本地出口链路需要单列；用户已选择先使用 Mac 模拟；完整链路仍须重测并单列网络影响。
- `f36fe42` 的 CI 与 Browser E2E 成功，Storybook 失败暴露了测试自身的 VIP 刷新/160 ms 动画竞态。候选让 VIP 仅在指定阶段变化，同时验证刷新前后允许的当前题、回到原题后的可见性与透明度；用户中心 49 项故事测试通过。新候选的全部远端 CI 仍需等待。
- 工作流新增 500 / 1,000 / 2,000 人真实 API 撮合和揭晓，逐级核验预期周期、配对数、参与人唯一性、冻结资料、引荐数量和全量首页快照；不能把 201 当业务完成。

### 独立 Linux 后续发现

`e8947e7` 已修复 500 人配对写入，但揭晓后的全量首页快照事务约 6.3 秒，超过默认 5 秒而回滚；邮件领取事务也在等待同一批 Match 锁时超时。候选为这两处设置 30 秒事务上限，并先完成首页快照再启动即时邮件领取，保持注销/举报与发信的行锁检查。引荐邮件也改用撮合时已冻结的介绍，避免准备后重填问卷导致邮件与卡片资料不一致。

工作流中 `docker run | tee` 曾使验收脚本的非零退出码被日志命令覆盖；现已显式使用 Bash `pipefail`。以业务断言、数据库和 API 日志判定该轮演练失败，不采用旧工作流的绿色状态。

`91589de` 的独立 Linux 演练完成了 500 人（250 对、500 快照）和 1,000 人（500 对、1,000 快照）的准备与揭晓，准备分别约 14.1 / 27.0 秒，揭晓及快照约 12.3 / 12.8 秒；同期健康请求最大 23 / 15 毫秒。2,000 人触发 120 秒 worker 超时，整轮未通过。该 SHA 的 Browser E2E 和 Storybook 成功，CI 构建成功，但 API E2E 因缺少 Mailpit 配置失败；后续已补齐 CI 服务和环境变量。

后续候选缓存同一问卷版本的题目比较与答卷规范化结果，释放候选临时索引，删除求解前的重复索引。求解库保留原算法、权重和遍历顺序，仅将逐元素数组填充替换为原生填充；MIT 源码及变更说明保存在 `apps/api/src/vendor/edmonds-blossom/`，400 组新旧配对数组比较完全一致。本地带历史及连续未匹配优先级的 2,000 人诊断约从 48 秒降到 28 秒，仍须通过限资源容器和真实 API 复验，不能据此声明生产容量达标。55 项相关单测、类型检查和构建通过；新版本的 500 人数据库与维护恢复测试通过（`artifacts/e2e/26e055eea01a/`）。

新增真实 PostgreSQL/Mailpit 演练已通过（`artifacts/e2e/4334b40e167b/`）：维护期间，即使后台和邮件开关开启，到期周期仍保持 OPEN、邮件保持 PENDING/0 次尝试、过期事件不清理；关闭维护但保留暂停开关也同样不写。恢复全部开关后，周期揭晓、事件按原保留政策清理、邮件变为 SENT 且 Mailpit 实际收到。500 人完整揭晓生成 500 条首页快照、500 封含冻结介绍的邮件；准备后改写当前介绍不会改变邮件内容。

## 2026-09-21 最新状态

候选 `0a17c6c7988e9354cd5a5385bafabd8b438249b1` 的两次 CI 和 Storybook 全部通过。独立 Linux/Neon 工作流 `35521074679` 完成三档真实业务，但容量断言失败：

| 人数 | 准备 | 配对数 / 揭晓快照 | 揭晓含快照 | 同期健康请求最大延迟 |
| --- | --- | --- | --- | --- |
| 500 | 11,161 ms | 250 / 500 | 8,945 ms | 6 ms |
| 1,000 | 15,871 ms | 500 / 1,000 | 12,854 ms | 74 ms |
| 2,000 | 117,907 ms | 1,000 / 2,000 | 15,457 ms | 235 ms |

2,000 人候选计算 15,231 ms、求解 88,699 ms；超过准备 60 秒门槛，工作流明确失败且未开放测试代理。合成数据包含大量相同软性答案，保留历次演练配对排除；不同轮次的历史边集不同，不能将耗时变化全归因于代码。它验证了业务完成和密集候选压力，没有证明真实用户答案分布或生产容量。

该提交 Browser E2E `35521043836` 因移动 WebKit 注册后重新登录偶发失败而未通过（首次失败、重试通过，仍按失败处理）。轨迹没有登录 POST，点击时页面滚动从 37 px 继续至 80 px；页面全局平滑滚动没有响应减少动态效果设置。后续候选增加该设置下的即时滚动及登录响应、最终页面回归。两条相关移动 WebKit 流程各重复 12 次，24 次通过且没有重试，记录 `artifacts/e2e/44defeb7cdf2/`；随后 Chromium、移动 Chromium、WebKit、移动 WebKit 的完整 88 项回归全部通过，记录 `artifacts/e2e/d066e049a70f/`。候选 CI 随提交继续核验。内置浏览器已检查 1440×900 和 390×844 登录布局，移动页面无横向溢出；这不代表真实 iPhone 验证。

已结束远端演练任务并暂停合成 Neon 计算节点，生产副本演练分支也已暂停；保留加密备份与复跑材料。没有创建付费测试机，Mac 临时网络路由已经恢复。生产 API 镜像、数据库与流量未切换。Vercel 内置浏览器仍在登录页，尚未具备隔离前端部署和 SSR 验收证据。完整 33/67/100 req/s、混合写入、30 分钟持续运行和生产受控验收仍未通过，不能发布。

### Mac 连接真实 Neon 的后续演练

在独立演练分支 `br-curly-feather-aoc2i9ik` 新建空数据库 `lilink_load_20260921`，专用普通账号 `release_mac_20260921` 不属于 `neon_superuser`，不能读取原副本 `neondb.User`。它仅拥有空合成库；入口白名单同时验证项目、分支、地址、数据库和账号。Mac 容器成功执行 44 条迁移并建立 2,000 个合成用户、2,000 份答卷、3 轮合成历史。

候选 `44f42f1` 的 Mac Linux arm64 容器限制 2 CPU / 3.5 GiB，500/1,000/2,000 人准备分别为 8,160 / 10,766 / 41,898 ms；全部生成 N/2 对和 N 条揭晓快照。准备期健康请求最大 8 / 20 / 48 ms。2,000 人候选计算 6,839 ms、求解 22,750 ms；该环境的准备目标通过。揭晓连同快照总耗时为 16,997 / 26,936 / 45,244 ms，尚未单列事务和队列排空，不能将总耗时当作揭晓事务耗时或完整容量通过。

首页冒烟完成 31 位用户 / 124 请求，业务断言全部成功、0 次意外 429、0 个丢弃迭代；读取 P95 1,048 ms，未满足 800 ms。继续以 33 请求/秒检验热态负载时发生大量 500 与超时，已主动停止，没有继续加压。日志指向联系方式读取等待事务连接超时（P2028），容器未 OOM 或重启。完整证据为 `mac-matching-44f42f1.log`、`mac-api-44f42f1.log`、`mac-ingress-44f42f1.jsonl` 和 `mac-load-*.json`，其中中止的测试不能按原计划完成量验收。

后续候选把联系方式的多往返只读事务改成单条 SQL，在同一 MVCC 快照内取得版本和联系方式，保留并发一致性及不存在账号的 404；5 项真实 PostgreSQL 并发测试和 42 项账号单测通过。仍需以新镜像重跑压力测试。实验性求解器优化只留在忽略目录，未进入候选。生产仍未改变，Vercel 管理登录、隔离 SSR 链路、完整压力矩阵和生产受控验收尚未完成。

### 正常后台任务下的 Mac 压测

`27228c4` 使用真实限流，恢复正常后台任务和 Mailpit 发送。每分钟 500 位首页用户、持续 120 秒，实际完成 1,001 次迭代、4,004 个 API 请求；999 位用户完整成功、4,001 个 API 请求成功，0 丢弃、0 意外 429。启动阶段有 3 个数据库连接中断导致的 500；读取 P95 3,491 ms、四接口整组 P95 4,556 ms，延迟门槛失败。直接进入匹配页的另一轮为每分钟 500 人、持续 60 秒，501 位用户及 501 个请求全部成功；P95 997 ms，仍未达到 800 ms，不能混同首页四接口流量。

只读诊断在独立空合成库运行：30 次 `SELECT 1` 的 P50 89 ms、P95 103 ms；匹配页压测期间另一组 P50 81 ms、P95 85 ms。热态首页汇总执行 13 条 SQL，问卷 5 条，联系方式 1 条。后续候选移除当前问卷重复加载版本和题目的两次查询，并让券包查询与其他首页资料并行；保持当前版本检查和答卷注意项。42 项账号单测、6 项 PostgreSQL 问卷/VIP 回归通过（`artifacts/e2e/f71153c7a546/`）。新候选仍须重测，不以查询次数减少推断容量通过。

本轮完整日志、入口请求记录及资源状态为 `mac-api-27228c4.log`、`mac-ingress-27228c4.jsonl`、`mac-resource-27228c4.json`；负载摘要为 `mac-load-home-500-120-1m-27228c4.json` 和 `mac-load-match-500-60-1m-27228c4.json`。Mailpit 保存了本次 API 启动后收到的 500 封测试邮件；更早已发送的邮件在此前收件器中，不能把新收件器计数当作全部 3,500 封邮件的收件证明。生产没有切换。

## Vercel 隔离 Preview 接通与性能复验

用户完成 Vercel 登录后，已为 `codex/questionnaire-reset-release` 配置独立 Preview 域名 `release-20260920.lilink.top` 和 API `release-api-20260920.lilink.top`。API 通过 Mac 的 Cloudflare Tunnel 连接独立合成数据库；用户、管理员、商户使用独立会话 Cookie，Preview 不发送 Sentry 事件。生产域名、生产数据库和 API 容器未切换。`a1ccc0f9d5e2a869fac1804c59671dbc8f536918` 的 Vercel Preview、两次 CI、Browser E2E、Storybook 均成功。

内置浏览器实际登录后能看到合成用户首页和新版完整问卷。Node.js + Playwright 经限定测试域名、一小时有效的临时访问凭据执行远端链路：已确认昵称保存及刷新、报名及刷新、取消报名和 API 回读；整套远端浏览器验收尚未通过。首轮保存等待超过测试默认 5 秒；实际 PUT 耗时约 9.4 秒。另一轮保存刷新时 API 发生 `Connection terminated unexpectedly`，页面显示“暂时无法加载”，已记录为失败。测试断言中旧的“过往匹配记录”文本已按实际“过往匹配”标题校正。失败报告保留，不能用后续重跑覆盖。

`a1ccc0f` 在正常后台任务和真实限流下，每分钟 500 位首页用户、120 秒完成 1,001 位用户 / 4,004 次 API 请求，全部业务成功，0 意外 429、0 丢弃迭代。bootstrap P95 989 ms / P99 1,586 ms，问卷 P95 634 ms、联系方式 409 ms，四接口整组 P95 995 ms；仍因 bootstrap 超过 800 ms 未通过。数据为 `mac-load-home-500-120-1m-a1ccc0f.json`，服务端请求与资源记录为 `mac-ingress-a1ccc0f.jsonl`、`mac-resource-a1ccc0f.json`。

后续候选合并首页快照读取，完整快照直接复用，缺失时仍等待修复并重读；当前问卷完成状态直接按活动版本筛选，避免加载旧答案与额外版本查询。昵称保存改为在匹配锁内批量更新快照昵称，保留历史资料与联系方式内容，不逐轮重建。55 项账号/快照单测和 21 项真实 PostgreSQL 问卷、VIP、揭晓、注销与隐私测试通过（`artifacts/e2e/308c12198b70/`），包含昵称/空昵称更新后其余快照字段保持、注销限制保留；新镜像和完整容量矩阵仍需复测。

### 首页优化与远端浏览器终态

`fbcea1d187b23388b8be3786094f4c00e8b7feb4` 进一步复用已读取的快照覆盖信息；没有参加某一历史轮次的用户不再触发无意义的重复读取。56 项账号/快照单测、19 项 PostgreSQL 回归通过（`artifacts/e2e/870ac2e070cc/`）；该提交的两次 CI、Browser E2E、Storybook 全部成功。

正常后台任务和真实限流下，每分钟 500 位首页用户持续 120 秒，完成 1,000 位用户、4,000 次成功 API 请求，0 错误、0 意外 429、0 丢弃。bootstrap P95 715 ms、问卷 545 ms、联系方式 307 ms、公开问卷 8 ms，四接口整组 P95 743 ms；API 入口日志逐接口各 1,000 次，与发压端完全一致。证据为 `mac-load-home-500-120-1m-fbcea1d.json` 和 `home-ingress-fbcea1d.json`。

同一提交提高到每分钟 1,000 人时出现持续排队和 10 秒超时，约 46 秒主动停止：仅 154 次完整业务成功，1,304 次 API 请求成功，不能按计划完成量验收。公开问卷仍快，依赖数据库的读取失速；容器无 OOM/重启，采样未见数据库锁等待。根因仍需连接池等待和查询往返数据定位，不能仅归因于网络或增加连接数。保留 `mac-load-home-1000-120-1m-fbcea1d.json`、`home1000-ingress-fbcea1d.json`。

独立 Vercel Preview 的 Node.js + Playwright 已分别通过 Chromium 桌面和 WebKit 移动完整流程：登录及 SSR 首页、问卷昵称保存/刷新、报名与取消后的 UI/API 回读、打开来信并核对匹配对象、刷新保持已读、历史记录入口、无横向溢出。最终两轮无 429/5xx，测试昵称及报名状态已恢复；证据目录为 `preview-browser-fbcea1d-1789927129484/` 和 `preview-browser-fbcea1d-1789927193432/`。先前失败记录仍保留，包括未先开信的移动断言，以及测试工具直连 Cloudflare 时的 TLS 中断。回读改为浏览器自身发起请求，未增加失败自动重试。

仍须完成更高负载、混合写入、持续运行、撮合与业务请求并发、完整邮件实收、Vercel SSR 容量及计时恢复演练；生产继续保持原版本。

### 连接池对比与计时恢复

`e370594` 的观测确认每分钟 1,000 人时 20 条连接用满，等待请求从 133 增至 447；单条查询往返多数约 100 ms。隔离库现场 `max_connections=901`，仅在测试 API 对比 40 和 60 条连接，生产未调整。40 连接完成 1,997/2,000 次首页业务、7,997/8,000 次 API，bootstrap P95 2,085 ms；60 连接完成 2,000/2,001 次业务、8,003/8,004 次 API，bootstrap P95 1,404 ms。两轮均无 429、无丢弃，但延迟仍不通过。60 连接下大多数采样已无排队，继续减少数据库往返而非无限增加连接数。分别保留 `mac-load-home-1000-120-1m-e370594-pool40.json`、`...-pool60.json` 及对应观测日志。

同一独立演练分支的生产副本 `neondb` 完成带计时恢复：停测试 API、校验并解密备份、完整恢复、聚合摘要核对、旧生产 amd64 镜像启动、原账号登录、旧答卷及最近三轮匹配读取，总计 **99,481 ms**。其中恢复 82,314 ms，旧 API 启动 6,313 ms，登录与业务回读 7,414 ms；低于数据库与 API 恢复 30 分钟目标。此计时不含前端回滚。随后重新执行候选迁移，44 条账本完成、252 条原样归档、252 份空白新版、654 份冻结历史以及全部非问卷聚合摘要核对通过。证据为 `timed-restore-1789928028355.json`。首轮因选到没有最近可见匹配的测试账号，历史断言失败；记录保留，后续从备份内已有可见匹配的账号中选择，未制造匹配结果。受控复制账号的临时口令和活动时间已还原，生产账号没有修改。

后续候选将首页当前轮次、最近三轮、用户最近一次参与和当前报名合并为一次 SQL，在同一数据库快照读取；保留最近三轮之外的最后参与、退出报名与空报名语义。43 项账号单测、19 项问卷/揭晓/隐私 PostgreSQL 回归通过；另一个真实 PostgreSQL 用例核对不同用户报名隔离、三轮之外的历史、时间类型和揭晓后立即读到新状态（`artifacts/e2e/634b1b86e689/`）。查询变更仍需新镜像容量复验。`e370594` 的全部四项远端检查均成功。

`5fd63fb` 使用 40 连接、重启后直接以每分钟 1,000 人进入首页，120 秒完成 1,997/2,001 次完整业务、7,998/8,004 次 API，0 意外 429、0 丢弃。bootstrap P95 836 ms，问卷 699 ms，联系方式 443 ms；仍因 bootstrap 超过 800 ms 失败。后续单列 30 秒预热时有 3/501 次业务因建连中断失败，故自动停止，未开始热态正式测量。观测显示错误发生在连接领取阶段，查询执行错误数为 0，不能把失败预热省略后宣称通过。

后续候选只对连接领取中的 ECONNRESET、EPIPE 和连接意外终止重连一次，不重试已开始的 SQL、事务、鉴权配置错误或连接池超时。14 项连接池单测、类型和 lint 检查通过；真实 PostgreSQL TCP 故障注入验证首次连接失败可恢复，已提交写入的响应丢失时返回错误且计数只增加一次，Prisma 断开后重新连接仍可读（`artifacts/e2e/879cffee4276/`）。其余 114 项 PostgreSQL 用例在前一轮通过（`artifacts/e2e/617297828502/`）；该轮新故障测试暴露 Jest VM 原生 Error 的跨上下文判断问题，已改成按错误字段识别并单独复验通过。新镜像仍需复测。
