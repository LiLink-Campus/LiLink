# 轮次列表常驻与统一编辑

本次根据轮次中心的后续反馈调整，覆盖此前验收记录中的折叠列表、参与者明细及「更多操作」展示方式。

- 轮次管理区常驻显示搜索、状态筛选与轮次卡片。桌面与手机均为单列（按后续反馈调整），当前轮次通过选中样式区分；超过一页时保留分页。
- 「新建轮次」「编辑轮次」并列放在轮次管理区，共用一个表单抽屉。原「轮次设置」改称「编辑轮次」。
- 删除参与者明细、筛选和分页及对应前端数据请求，保留统计图表。
- 「强制重新执行」与「正式执行」并列展示，保留影响说明和原有二次确认，不改变执行逻辑。
- 通过真实本地页面新建「演示-秋日来信」「演示-周末相遇」「演示-校园初见」3 个草稿，并保存一个演示轮次的备注验证编辑流程。与原有轮次共 4 个，未执行匹配。

## 验证

- Web 与 Storybook 类型检查、CSS 语法及本次范围差异检查通过。
- 8 项轮次 Storybook Chromium 交互通过，包括多轮次选择、共用创建/编辑表单、常驻执行按钮、明细移除、预演与最终切换及错误状态。
- 真实本地页面完成新建、编辑保存、重新加载确认、草稿/已揭晓筛选，以及 1440、942、901、900、683、651、650、390 px 的列表、执行区和表单抽屉检查，无页面横向溢出或脚本异常。
- 网络检查确认不再请求轮次参与者明细，验证期间没有调用正式/强制匹配接口。

证据位于忽略目录 `artifacts/admin-cycles-flat-2026-09-16/`：

- [桌面多轮次](../../artifacts/admin-cycles-flat-2026-09-16/cycles-942.png)
- [手机多轮次](../../artifacts/admin-cycles-flat-2026-09-16/cycles-390.png)
- [统一编辑表单](../../artifacts/admin-cycles-flat-2026-09-16/editor-942.png)
- [浏览器检查结果](../../artifacts/admin-cycles-flat-2026-09-16/browser-results.json)

仅本地修改与演示数据，没有提交、推送或部署。
