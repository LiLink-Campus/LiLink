# 1 对 1 恋爱匹配活动页

本地页面：`/one-to-one`。登录首页「了解一对一服务」已从弹窗改为独立页面链接。

## 内容

- 专人牵线：299 元/人，联系方式交换、三人小群介绍、首次破冰。
- 7 天恋爱挑战：599 元/人，增加首次约会介绍与安排、对应约会套餐、7 天互动与运营跟进。
- 双方愿意认识后再付费；在一起并完成网名故事投稿，每人返 300 元。
- 约会套餐、返还细则尚待确认；登记及收款未开放。按钮定位到登记说明，不提交数据。

## 本地验收

- Playwright 实际登录并从首页入口进入活动页。
- 395、700、768、900、1440px 页面无横向溢出。
- 套餐说明可展开；登记按钮定位到尚未开放说明。
- 手机与桌面截图：`/tmp/one-to-one-final-395.png`、`/tmp/one-to-one-final-1440.png`（本机临时证据）。
- Web 类型检查、Storybook 类型检查、变更文件 ESLint、CSS 语法检查通过。
- 新增 Storybook 展示与说明展开场景；未运行完整 Storybook 测试套件。

尚未实现报名、付款、退款及运营履约后端；本次为活动介绍页与入口迭代。未提交、推送或部署。

## 文案与返回入口调整

返回首页移至左侧，删除重复活动标题；返还卡片去掉“网名”字样，投稿匿名规则仍在说明中保留。首屏、流程、费用说明与底部入口统一强调登记和看介绍免费，只有双方互选且都愿意进一步了解时，才付费交换联系方式。366、395、1440px 浏览器验证无横向溢出，变更文件 ESLint 与 CSS 检查通过。

## 活动 FAQ 版式对齐

活动说明直接复用首页 `faq.module.css` 的居中标题、720px 内容宽度、分隔线、右侧折叠箭头与响应式间距；同组问题一次展开一个，原说明内容保留。366px、1440px 浏览器检查展开和切换通过，无横向溢出；ESLint、CSS 语法检查通过。截图保存在 `/tmp/lilink-faq-{366,1440}-{closed,open}.png`。

## 流程区合并

删除底部登记卡片及独立四步区，将四步内容合并到原体验区排版：手机单列编号、桌面两列四项。“候选人的介绍”改为“候选人介绍”。`#participate` 现定位到合并后的流程说明，原入口仍有效。372px、1440px 验证四项展示、卡片删除与无横向溢出；ESLint、CSS 和 Storybook 类型检查通过。截图 `/tmp/lilink-merged-flow-372.png`、`/tmp/lilink-merged-flow-1440.png`。

## 首屏简化与底部留白

移除首屏渐隐叠图，复用清晰校园插画，桌面图文分栏、手机文字与横图分开，按钮不覆盖画面。页面底部 padding 减半（手机 110→55px，桌面 48→24px），FAQ 底部 padding 同步减半，仅作用于本页。372、768、1440px 浏览器无横向溢出；首屏与底部截图已检查，ESLint、CSS 语法及 Web 类型检查通过。证据 `/tmp/lilink-clean-hero-372.png`、`/tmp/lilink-clean-hero-1440.png`、`/tmp/lilink-bottom-spacing.png`。

## 首屏插画重新生成及说明去重

重新生成简约长椅人物插画并保存为 `apps/web/public/images/one-to-one-minimal.webp`，置于首屏文案下层，取消独立卡片边框，底色融合。免费查看、双向意愿、按人收费与单向不收费说明统一到首屏，删除套餐旁重复说明，桌面套餐改为两列。372px 与 1440px 实际截图检查通过，无横向溢出；登记说明锚点可达；ESLint 与 CSS 检查通过。证据 `/tmp/lilink-minimal-372.png`、`/tmp/lilink-minimal-desktop-final.png`。

## 按原视觉稿重新融合首屏

按用户重新指定的初版视觉稿恢复咖啡约会插画：桌面以横幅留白融合文案，手机居中标题、插画融入首屏且按钮位于画面底部，套餐紧接首屏。保留已确认的文案、四步合并及 FAQ 删减。366、372、768、1024、1440px 验证无横向溢出，登记说明锚点可达；ESLint、CSS 与 Web 类型检查通过。截图 `/tmp/lilink-reference-372.png`、`/tmp/lilink-reference-1440.png`。
