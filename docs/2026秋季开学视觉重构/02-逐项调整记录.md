# 逐项调整记录

## 2026-09-08：背景改为浅米色

用户反馈原背景偏棕黄，希望更偏米色。本次只改副本的背景色调。

| 用途 | 原颜色 | 新颜色 |
| --- | --- | --- |
| 全站底色 | `#fdf8f0` | `#faf9f3` |
| 次级背景、页脚等 | `#f1e8d8` | `#f0eee5` |
| 浅色衬底 | `#fcf6ec` | `#f7f6ef` |

首页去掉覆盖在底色上的珊瑚红、金色和绿色径向渐变，让首页与其他页面保持同一米色底。浏览器主题色、PWA 启动底色、离线页和错误页底色同步为 `#faf9f3`。

品牌酒红、卡片白色、文字、页面布局与交互未调整。当前为第一轮颜色方案，等待用户视觉反馈。

## 2026-09-08：按 CampusDate 调整顶部与页脚

参考：[CampusDate 交大首页](https://trycampusdate.com/sjtu)。修改前已读取页面 HTML 及实际加载的 `index-Cb0dhm-h.js`、`index-BaaOpo1n.css`，确认其页脚由品牌、分组导航、关注入口、底部分隔信息组成；桌面横向排列，768px 以下纵向展开。

本轮只调整副本中的这些部分：

- 删除首页学期公告及其独占样式。
- 顶部只保留“关于我们 / FAQ / 登录”；已登录用户的“登录”替换为“我的匹配”，退出入口移至页脚。
- 页脚左侧为 LiLink 与品牌标语，中间分为探索、支持、关于，右侧为关注我们。
- 学校、注册、更新、用户协议、隐私等保留为真实页面链接，更新未读标记也移到页脚。
- 微信、小红书在页脚并排展示，当前为明确标注的二维码占位；未使用参考站的账号、二维码或备案信息。
- 继续使用当前浅米色，不复制参考站的深棕底色；其余首页内容及业务流程保持原样。

验证：副本类型检查通过；三个修改的 CSS 文件通过仓库解析器检查；浏览器确认公告不存在、顶部只有三项，电脑和 390px 手机页脚无横向溢出。手机菜单可展开并显示三项入口。

## 2026-09-08：飞鸟衔信 SVG 品牌图标

依据用户提供的 LiLink Logo，保留向右飞翔的鸟、展开的羽翼、信封与爱心。用 SVG 路径手工重画，采用酒红描边、浅粉羽翼、脸颊和爱心细节；没有嵌入 PNG、外部位图或字体轮廓依赖。

- 母版：`web/public/icons/icon.svg`，透明背景，256 × 256 viewBox。
- 应用裁切版：`web/public/icons/icon-maskable.svg`，米色底，主体缩到安全区域。
- 页头、页脚、用户应用外壳、管理后台标记、离线页、浏览器 favicon 与 manifest 均引用新 SVG。
- 旧 PNG 图标和 Next.js 自动 favicon.ico 已移出副本，放入废纸篓；正式 `apps/web` 未改。
- `npm run pwa:icons` 现在从 SVG 母版生成 SVG 裁切版，不再生成 PNG 或重建旧的 Li 色块。
- 更新 Service Worker 图标预缓存路径和缓存版本。

验证：类型、CSS 与 SVG XML 检查通过；两份 SVG 均含 11 个路径且没有 image/foreignObject。浏览器核对了桌面和 390px 手机页头、页脚图标、SVG favicon 引用及离线页；管理员和用户应用外壳按共享图标组件替换，未在本轮登录账号检查这些受保护页面。

兼容边界：本轮依照用户要求保持纯 SVG，不提供 PNG 备用。Apple 的 [Web Clip 图标文档](https://developer.apple.com/library/archive/documentation/AppleApplications/Reference/SafariWebContent/ConfiguringWebApplications/ConfiguringWebApplications.html) 指定 PNG，因此移除了旧 apple-touch-icon 声明，不把 iPhone 添加到主屏幕的图标兼容或真实 PWA 安装算作本轮已验收。

### 同日修正：恢复原 Logo 单色与整体对齐

用户反馈粉色过多、图标视觉上偏下。以原图取色结果为依据改为灰棕色 `#74685c`，去掉粉色羽翼、脸颊、眼睛高光、外侧爱心和星形装饰，仅保留飞鸟与爱心信封线稿。品牌文字同步为相同灰棕色。

图标相对右侧完整文字组上移 3px，仍保留容器的垂直居中；页脚同样作视觉补偿。同步生成应用 SVG 并更新图标缓存版本。此修正替代上一版粉色 Logo 方向。

### 后续修正：以品牌名称为对齐基准

用户仍觉得偏下。测量确认 SVG 线条重心约在画布 y=145.54，低于 256 画布的中心 128；先前对齐整个名称和标语，又让主要视觉重心落在名称下方。

将横向 BrandMark 改为两行网格：飞鸟与“LiLink”共享首行，标语独立位于第二行。图标尺寸改为 44px，保留 3px 光学补偿，整个品牌区仍高 52px。浏览器实测飞鸟线条重心 y=31.61px、名称行中心 y=31.59px，头部高度不变。紧凑版与竖排版维持各自布局。

用户随后反馈该版偏上：仅将带标语的横向标记中图标的偏移从 -3px 调为 +1px，即相对上一版下移 4px；配色、图形大小、文字及头部高度不变。此处以用户的视觉反馈为准，不再把墨迹重心对齐等同于视觉验收。

用户进一步指定再下移 1.5px：偏移由 +1px 调为 +2.5px，以图标与右侧文字的垂直视觉中心对齐为目标，其他属性不变。

随后按用户要求继续下移 0.5px，当前偏移为 +3px。

再次按用户要求下移 0.3px，当前偏移为 +3.3px；布局基准保持不变。

最终按用户要求再下移 0.2px，确认带标语的横向 Logo 使用 `translateY(3.5px)`。这是当前 44px 图标、36px 主行和 16px 标语行结构下确认的光学位置；后续微调保留此值，不重新按外框居中或随意更换对齐基准。

用户随后要求再向下移动 0.3px，最新偏移更新为 `translateY(3.8px)`，替代此前的 3.5px；其余尺寸和布局保持不变。

用户最后要求再下移 0.2px，当前确认值为 `translateY(4px)`，替代此前的 3.8px；其余属性不变。

## 2026-09-08：FAQ 移到首页底部，移除最近更新

按用户新标注调整首页。读取并核对 CampusDate 实际加载的 `LandingView-DPXi9OJt.js` 和 `LandingView-4LSccrp9.css`：FAQ 列表最大宽度 720px，标题居中，问题为细分隔线列表，右侧箭头随展开旋转，同一时间展开一项。

- 删除首页“最近更新”展示区及首页对应的 devlog 数据读取；页脚更新日志入口与原更新页面保留。
- 将原有 9 个 FAQ 的内容移入 `FaqSection`，放在首页理念区之后、页脚之前，不替换成参考站的问题或平台承诺。
- 采用居中标题、无卡片背景的分隔列表、原生 details/summary 和旋转箭头。长答案自然展开，不设置会截断内容的固定最大高度。
- 顶部及页脚 FAQ 链接改为 `/#faq`；旧 `/faq` 路径跳转到该位置，保留原有深链可用性。
- 浅米色背景和 Logo 的 4px 偏移保持不变。

验证：类型与 CSS 检查通过；浏览器确认 9 个问题存在，展开第二项时前一项关闭；首页不再显示最近更新；旧 FAQ 路径成功跳转。390px 手机页面无横向溢出，长答案显示高度与内容高度一致。

FAQ 导航使用原生锚点链接，保证已经位于 `#faq` 地址但滚动离开后，再次点击也能回到 FAQ 标题；其他导航继续使用原有 Next Link。

## 2026-09-08：顶部增加学校入口，统一“常见问题”措辞

按用户最新标注，顶部第二项从 FAQ 改为“支持的学校”，链接到 `/schools`。顶部当前为“关于我们 / 支持的学校 / 登录”。页脚原 FAQ 链接改称“常见问题”，继续定位首页底部；页面对用户展示的 FAQ 英文措辞已清除。内部组件名及 `#faq` 锚点保留，避免影响已有链接。Logo 对齐和其他布局不变。

## 2026-09-08：突出倒计时与统计信息

继续参考 [CampusDate](https://trycampusdate.com/sjtu) 已核对的实际页面资源：独立居中的倒计时、数字上方级别更高、单位和说明下置；统计区为居中的大数字与下方标签，手机端纵排。

- 将倒计时从首屏文字旁的小提示移到首屏下方独立区域，数字按天、时、分、秒四组展示，桌面最大 56px，手机约 37–40px。
- 补充明确的揭晓日期和北京时间。沿用现有时间计算、未配置、数据不可用及已揭晓的处理，秒数仍正常更新。
- 统计区移除小白卡片，数字放大到约 44–60px，标签下置并提高对比。“正在准备首轮匹配”仍按文字状态展示，不修改统计值及原有增量逻辑。
- 手机统计项纵向排列，避免文字状态与数字挤在同一窄行。倒计时取消每秒的屏幕阅读器实时播报，保留可读取的计时内容。
- 品牌、Logo 的 4px 偏移、其余页面内容和业务逻辑不变。

验证：类型与 CSS 检查通过；浏览器核对了桌面、390px 手机和 320px 窄屏，未见横向溢出；倒计时秒数持续变化，准确揭晓日期与现有轮次一致。统计值仍为当前本地页面原有的 50+、40 和首轮筹备状态。

用户随后要求将平台数据上移，与倒计时放在一起。首页顺序调整为：首屏 → 倒计时与揭晓日期 → 平台数据 → 相遇方式 → 后续内容。收紧倒计时与统计区间距，数字样式、手机纵排和数据口径保持不变。

## 2026-09-08：暂不做多人局

用户明确多人局复杂度过高，暂不实施。首页删除多人局卡片、即将开放预告和“选择一种相遇方式”标题，移除双列选择布局，让现有 1v1 匹配入口完整占据该区域。保留原 1v1 路由、数据和交互；不涉及现有一对一见面安排功能。

## 2026-09-08：按 CampusDate 整页布局重排

用户明确中间的单独 1v1 卡片也不需要，要求直接模仿 CampusDate 布局。重新核对当前参考入口与已取得的同版本 CSS、JS，采用其整页分区和网格规则：居中大首屏、倒计时与统计、三列流程卡、两列特点、常见问题、结束处加入区、分组页脚。

- 删除独立 1v1 入口卡及 ModeSelectCard 的首页引用。
- 首屏改为居中排版，约一屏高度，保留原主标题与本地校园线稿；主按钮和次要文字链接区分权重。
- 将现有参与步骤整理成三张卡：邮箱验证与资料、确认参与与等待、查看结果与联系。没有修改实际注册或参与接口。
- 将原理念内容纳入统一的平台特点区，使用两列排版；保留原理念说明。
- 常见问题后增设“准备好了吗”加入区，链接到现有注册页面。
- 统一分区标题、卡片、间距与中文自然换行。图片区使用 LiLink 现有 SVG 线稿，未采用参考站的校园照片或品牌素材。
- 保留浅米色、Logo 的 4px 光学偏移、现有导航和页脚；统计口径不变。

验证：类型检查及 CSS 解析通过。浏览器确认三张步骤卡、四个特点、九个常见问题和结束处加入入口；已点开常见问题并通过底部按钮进入原注册页。修复步骤卡宽高比导致的窄屏溢出后，390px、320px 均无横向溢出，步骤正文完整显示。

## 2026-09-08：动漫校园背景

首屏与底部加入区的 SVG 装饰替换为内置 imagegen 生成的动漫校园 WebP。按后续反馈，首屏添加并肩行走交谈的男女大学生，调整文字留白和渐变遮罩，保留人物完整呈现；底部采用傍晚校园湖景与白色文字。图片与完整提示词见 `03-动漫背景素材.md`。Logo、流程插画与业务数据保持原样。

验证：类型检查、CSS 解析通过；内置浏览器确认桌面及 390px 手机首屏人物不被文案遮挡、图片加载正常、底部标题为白色，桌面无横向溢出。

后续按用户上传海报重做首屏：暮紫天空、暖橙晚霞、男女主对视，使用 `campus-dusk-romance.webp`（约 136 KB）。标题和按钮改为米白色，保留原布局、Logo 与实际统计值。桌面和 390px 手机端确认人物面部与文案分离，无手机横向溢出。

再次依据反馈重新生成清晰自然色版本：移除夕阳、紫调、光晕与整图遮色；首屏改回深色文字和品牌色按钮。核验 CampusDate 当前公开 CSS/JS 后，以 120–240px 底部渐变衔接米色画布。桌面与 390px 手机浏览器已确认人物清晰、无文案遮脸、手机无横向溢出；CSS 解析通过。完整生成提示词与素材路径见 03 文档。

用户进一步明确可采用粉色与背景虚化。对照 CampusDate 实际首屏与已核验样式后，将首屏替换为玫瑰粉花树动漫情侣图，远景柔焦、人物表情清晰，底部增加 3px 局部虚化及渐淡遮罩。同步浅粉首屏底色，保留酒红品牌色与 Logo 位移。桌面、390px 手机已查看实际截图，无手机横向溢出，CSS 解析通过。素材与内置 imagegen 提示词存入 03 文档。

## 2026-09-09：参考原图构图转动漫

从 CampusDate 实际 DOM 获取首屏原图，按原图转绘为动漫，保持大片花冠、右侧树干、红砖楼、步道与小比例情侣的布局。替换为 campus-blossom-scene-anime.webp，保留铺满背景和 center 30% 裁切，玫瑰遮色与底部虚化渐淡。修正遮色层覆盖文字的层级问题与手机标题旧颜色。桌面及 390px 手机确认图像正常显示、无横向溢出；CSS 解析通过。

## 2026-09-09：无卡片合作高校展示

按用户名单展示 11 所高校，采用原色校徽与校名标识墙：桌面四列、末行居中，手机两列。移除原页面的大容器卡片、学校卡片和邮箱标签堆叠，保留米色与品牌字体。11 枚校徽采用 1024px 透明 PNG，来源见 04 文档。邮箱范围单独放入可展开的文本列表，继续读取后台真实配置，不以营销展示名单覆盖注册规则。

验证：类型及 CSS 检查通过；内置浏览器确认桌面、390px 手机展示，无横向溢出；11 枚校徽无加载失败。搜索 bupt.edu.cn 返回原有校区和两个邮箱后缀，不存在的后缀显示无结果提示。

## 2026-09-09：中外合作高校分组与邮箱核查

按用户表格展示 11 组中外合作关系，共 28 所高校、17 所境外伙伴标识。以中方高校及黎安办学载体为左侧，境外伙伴为右侧；手机上下排列，不使用卡片。搜索框删除，旧 API 测试名单替换为当前展示名单及公开资料整理的学生邮箱后缀。中国政法大学未确认学生后缀，明确标记待确认。仅变更视觉副本，未写入后台注册白名单；核查资料见 05 文档。

验证：类型检查、CSS 解析通过；浏览器确认 28 枚标识无加载失败、旧学校记录与搜索输入框均为零、邮箱展开可用，390px 手机无横向溢出。

## 2026-09-09：紧凑邮箱与分割线整理

邮箱改成一校一行，去除重复的中方校名与大列空白，缩短组内间距；手机以紧凑两行显示校名和后缀。取消模块首尾边线，邮箱仅相邻学校组之间留细线，末尾不再产生双线。密德萨斯更换为校方手册提取的彩色矢量标识。类型、CSS 检查通过；桌面及 390px 手机已核对，邮箱 11 组保留，无横向溢出。

邮箱进一步重排为横向分组网格：桌面三列、手机两列，每组内校名与后缀相邻，不再使用横跨全宽的纵向表格，也不加卡片。校徽合作区顶部和底部各恢复一条细线，模块间不叠加边线。浏览器确认桌面三列、390px 手机两列，无横向溢出，首末边线均为 1px；CSS 解析通过。

## 2026-09-09：手机居中与常驻邮箱区

手机端中方高校标识组合、底部加入文案和按钮居中。邮箱区改为常驻 section，去掉折叠按钮；保持桌面三列/手机两列，每组上方增加短细线区分。按用户确认补充北邮 @bupt.cn，与 @bupt.edu.cn 并列显示，未修改注册后台。手机导航参照已取得的 CampusDate CSS：白色菜单底、暖灰文字、米白按钮和中性当前项，替换原粉色背景高亮。

类型检查及两份 CSS 解析通过；445px 浏览器确认居中、导航开闭、两后缀可见、无折叠元素且无横向溢出。

## 2026-09-09：生图探索后的手机布局重构

按用户要求用内置 imagegen 探索手机学校与邮箱版式，设计图和提示词保存在 design-explorations/mobile-school-layout.*。采用中外高校左右对应、中方校徽及名称纵排、双境外伙伴右侧纵排的结构，保留真实官方标识，不使用生图里的近似校徽与错位文案。调整手机标题、标识比例与留白。邮箱取消组顶分割线，保持手机两列对齐与常驻展示。

验证：CSS 解析通过；445px 手机已查看高校分组与邮箱实际截图，11 组邮箱边线均为 0px；320px 窄屏无横向溢出。桌面布局规则保持原状。

2026-09-09 后续微调：邮箱宽屏三列按左/中/右对齐，手机双列按左/右对齐；保持分组内校名和后缀同向对齐，无分割线。高校展示到邮箱区间距为宽屏 64px / 手机 48px，邮箱到加入区为 80px / 64px。浏览器在 771px 与 543px 验证样式生效，手机无横向溢出。


## 2026-09-09：关于页参考 CampusDate 重排

核对参考站 `/about` 和当前公开资源 `AboutView-BsYVQb2s.js`、`AboutView-DOKwc5mP.css`，采用 880px 单栏、简洁页头、正文分区和移动端纵排。保留 LiLink 品牌与自身内容；关于页删除原特点卡片、四步流程、大引语卡和底部插画 CTA，改为故事与团队简介。成员资料与联系渠道尚未提供，未编造或复制参考站信息。

按首页标注移除特点模块下方独立分割线。按后续反馈删除关于页 FAQ，常见问题只保留首页现有入口。此前新增的 FAQ about 变体及样式同步移除。

副本类型检查、CSS PostCSS 解析通过；浏览器检查桌面及 390px 手机布局，手机无横向溢出。截图证据保存在本机 `/tmp/lilink-about-qa/`。


### 关于页页头与团队占位

页头替换为小红书、微信二维码占位，真实素材待提供。扩充介绍为三人团队与校园相遇的简短说明；团队增加三位成员，圆形动漫头像、姓名、职责及个人介绍均明确占位。桌面三列、平板两列、手机单列。副本类型检查和 CSS 解析通过，390px 浏览器确认无横向溢出，桌面与手机截图保存于 `/tmp/lilink-about-qa/about-team-desktop.png` 和 `/tmp/lilink-about-qa/about-team-mobile.png`。


### 团队说明下移与联系我们

公益运营说明移到三位成员介绍下方。新增独立「联系我们」分区，包含问题反馈、校园合作和加入团队的邀请，以及邮箱、微信、小红书待补充项。类型检查、CSS 解析通过，浏览器已确认段落顺序与联系区块，截图 `/tmp/lilink-about-qa/about-contact.png`。


### 底色对齐 CampusDate

读取参考站当前实际加载的 `index-BaaOpo1n.css`，并在 `/sjtu/about` 核对 computed style。副本全局 `--color-canvas` 从 `#FAF9F3` 改为参考站主底色 `#FAF8F5`；`--color-surface-muted` 从 `#F0EEE5` 改为参考站次级底色 `#F2EEE8`，沿用现有浅色页脚结构。CSS 解析通过，浏览器确认正文背景 RGB(250,248,245)、页脚 RGB(242,238,232)。截图 `/tmp/lilink-about-qa/about-background.png`。


### 紧凑间距与深色页脚

按用户反馈收紧关于页分区、段落和页尾留白。页脚采用 CampusDate 实际 computed style 确认的 `#3D1F17`，配浅色文字与反白飞鸟；导航继续独立白底。浏览器确认布局与页脚层次，CSS 解析通过。


### 水彩背景、联系图标与移动端重排

依据用户认可的生图设计稿，以内置 imagegen 生成三联水彩素材 `web/public/images/about/watercolor-atlas.png`：校园钟楼与花树、花枝、花瓣。CSS 按区域显示并柔化边缘，装饰元素不参与交互。联系区采用 Tabler mail/brand-wechat 和 Simple Icons xiaohongshu SVG，源码和许可证位于 `web/public/icons/contact/`。

手机端独立编排：正文正常中文换行，二维码并排配右侧校园插画；故事引语配小花枝；三人成员纵排，左头像右资料；联系方式带图标纵排。保留真实资料和二维码占位。浏览器核对 320、390、768、1200px，无横向溢出；390px 验证全部联系图标显示与成员排版。类型检查与 CSS 解析通过。截图 `/tmp/lilink-about-qa/about-watercolor-mobile.png`、`/tmp/lilink-about-qa/about-watercolor-desktop.png`。


### 装饰收进正文宽度与移动端修整

取消校园、花枝和花瓣的负向外扩定位。页头使用显式网格：桌面介绍与二维码在左、校园图在右；手机介绍通栏，二维码与小幅校园图并排。手机故事引语和花枝分列，不再叠加；成员与联系区统一左对齐。取消页面溢出裁切，浏览器测量确认 320px 和 1200px 三组图案均在正文容器边界内，320px 无横向溢出；390px 已查看页头、故事和成员排版。CSS 解析通过。截图 `/tmp/lilink-about-qa/about-contained-mobile.png`、`/tmp/lilink-about-qa/about-contained-desktop.png`。


### 生图重设计手机关于页

以用户手机截图调用 imagegen 生成独立移动端稿 `design-explorations/about-mobile-concept.png`。实际手机布局调整为居中三行简介、横向校园水彩图、两列关注占位、浅粉引语区及紧凑成员行；去掉移动端成员占位说明的冗余文字。保持桌面内容与真实信息占位。浏览器查看 390px 首屏和团队区、320px 窄屏，无横向溢出。副本类型检查、CSS 解析通过。截图 `/tmp/lilink-about-qa/about-mobile-redesign.png`。


## 2026-09-09：登录、双注册路径与免填写视觉预览

按四处浏览器标注统一调整登录、注册方式选择、学校邮箱和普通邮箱注册。保留暖白画布、酒红强调色与现有字体；卡片最大宽度 540px，细暖灰边框、轻阴影、小圆角输入框及按钮。删除登录/注册的枝叶角标和底部草线；登录改为「欢迎回来」，注册使用中文两步进度条。共享认证样式同步作用于找回密码页。

合作高校区移除嵌套卡片、粉色标题块与邮箱胶囊；默认收起，展开后提供搜索、校名和邮箱的细线列表。列表内部可滚动，避免展开全部学校后把主操作推到数屏之外；校名长文本可换行，保留搜索空结果和邮箱识别提示。

副本开发配置启用隔离的视觉预览：认证表单空白直接推进，用户页面使用虚拟身份和资料，提供页面跳转及四种匹配状态切换。资料完成度测试为 100%；见面安排预填候选时间地点。请求进入本地预览处理器，不改变原站认证、真实账户或数据库。范围和使用方法见 README。

验收：副本 Next 类型检查通过；预览隔离、完整资料与状态切换、高校匹配相关 8 项测试通过。仓库 CSS 命令跳过 docs 路径，改以 PostCSS 显式解析三个修改文件，均通过。浏览器验证空白登录、两种注册两步、用户主要页面直接访问、匹配等待状态、见面安排三步、高校搜索和空结果。320px、390px、1200px 相关页面无横向溢出；截图保存在 `/tmp/lilink-auth-qa/`。本轮没有提交或发布。


### 登录卡片单独简化

按用户反馈恢复柔和圆角，仅调整登录页：卡片最大宽度 460px、桌面 28px / 手机 24px 圆角，输入框 14px，按钮为胶囊。删除顶部标语、副标题、底部分隔线和阴影，标题居中；忘记密码移到密码标签右侧。使用登录页独立 CSS，注册页面维持原样，免填写预览继续保留。类型检查、CSS 解析通过；桌面和 390px 手机实际查看，手机无横向溢出，截图 `/tmp/lilink-auth-qa/login-simple.png` 和 `login-simple-mobile.png`。预览服务无响应后仅重启 3101 副本，已恢复。


### 2026-09-10：注册方式选择页紧凑化

仅调整 `/register`：删除底部登录入口上方的分隔线、顶部小标语及重复的继续注册文字。主卡片最大宽度 460px，圆角桌面 28px / 手机 24px；两种注册入口采用 20px 圆角，校名类型、条件标签与箭头同行，说明文案简化。通过独立 className 限定样式，后续两种注册表单不受影响。类型检查及 CSS 解析通过，桌面与 390px 手机已查看，手机无横向溢出；截图 `/tmp/lilink-auth-qa/register-compact-desktop.png`、`register-compact-mobile.png`。


## 2026-09-10：参考注册流程、邮箱验证码同屏与停止收集真实姓名

参考站实测：https://trycampusdate.com/sjtu/register 。读取当前公开 HTML、index-Cb0dhm-h.js、RegisterView-D2jTx6_f.js、RegisterView-B9ZkfFwi.css，确认其三步为邮箱验证、验证码身份验证、个人信息；CSS 使用最大 420px 表单、28px 圆形步骤标识、细连线及无页脚认证布局。后续步骤依据已公开 JS 确认，没有发送真实验证码或注册参考站账号。临时源码证据在 /tmp/campus-register.*。

按用户意见调整 LiLink 视觉副本：顶部显示注册方式、邮箱验证、设置账号三个阶段；两种路径均将邮箱与验证码放在同一屏，发送按钮在验证码输入框旁，发送后留在本页，保留重发冷却。下一步填写昵称、密码及普通邮箱所需学校信息；返回可改邮箱，邮箱变化清除原验证码状态，最终提交时后端校验验证码，校验失败回到邮箱步骤。顶部勾选表示步骤填写完成，不新增单独的验证码验证 API。注册方式页及两种注册流程均去掉页脚；保留既定柔和圆角和免填写视觉预览。

历史原因边界：原两步流程随 f162f36 非教育邮箱注册与风控改动引入，提交信息未说明分屏是防刷要求。当前实际安全控制在后端 auth-throttle.ts 与验证码消费校验，不依赖表单是否分屏。

真实姓名停止收集：从原站和副本的注册表单、提交 payload、后台编辑框删除；RegisterDto、UpdateProfileDto 不再接受 fullName，AuthService 新注册不再写入此字段。新增 ValidationPipe 测试，验证不带姓名可注册，带 fullName 的注册和资料更新请求被拒绝。已有数据库列、历史记录及后台只读历史展示保留，不执行历史数据删除或数据库迁移。此项源码改动包含 apps/web 和 apps/api，尚未提交或部署。

验收：API、原站 Web、视觉副本类型检查通过；54 项认证、限流、DTO 相关测试通过；CSS PostCSS 解析通过。浏览器验证两种流程、发送后同屏与冷却、前进返回、无真实姓名字段、无页脚，以及 320/390/1200px 无横向溢出。截图保存在 /tmp/lilink-registration-flow-qa/。


## 2026-09-10：认证校园视觉、居中布局与学校查询弹层

核对参考站当前公开 LoginView-CMnEBA6W.js、LoginView-paX-dH2e.css：桌面图片区约 45% 宽，768px 以下改为顶部横幅，手机高度约 160px。副本登录、注册、找回密码采用同类响应结构，复用既有动漫校园图及 LiLink 文案；保留品牌导航，不显示认证页页脚。桌面左右 42/58 分栏，手机顶部横幅，认证内容在可用高度内居中，长内容正常滚动。

按用户意见保留注册选择页圆角卡片，普通邮箱与学校邮箱表单去除外框及重复说明、底部重复路径引导，缩短返回链接。学校邮箱区域使用 imagegen 探索后落地：一行学校识别、独立查询按钮、原生 dialog 弹层，手机为底部 sheet，支持搜索、无结果、Esc 和关闭焦点返回。数据沿用现有学校配置，未采用生图虚构素材或数据。

补充完整邮箱检查：仅输入 @bupt.edu.cn 时提示补全账号，不显示学校可用；完整地址才识别学校。前端识别仍不等于邮箱所有权验证，后端校验保留。

验证：副本类型检查、13 项邮箱识别及预览相关测试、四份 CSS 解析通过。浏览器核对桌面 1200px、390px 手机和 320px 窄屏，无横向溢出；注册选择区上下留白实测一致；查询、空结果、Esc、焦点返回及免填写下一步可用。证据 /tmp/lilink-auth-simplified-qa/，生图稿保存在 design-explorations/registration-email-concept.png。此次仅改视觉副本，之前尚未提交的真实姓名相关修改保持原状。


## 2026-09-10：昵称移到登录后设置

按用户标注，学校邮箱和普通邮箱注册均移除昵称字段与提交状态，注册 DTO 不再接收昵称，注册服务以 null 创建昵称。复用登录后匹配资料／编辑名片的昵称入口，保留已有昵称和历史真实姓名数据。决策考虑、接口边界与验收见 [06-注册信息最小化与昵称后置](06-注册信息最小化与昵称后置.md)。API、原站与副本类型检查通过，46 项服务／DTO 测试和 10 项 HTTP 测试通过；本地源码未部署。


## 2026-09-10：注册点缀与桌面学校弹窗定位修复

按用户反馈，在注册进度上方复用现有飞鸟图形，配淡色短虚线作轻量点缀；装饰对辅助技术隐藏，不增加操作或文案。

实测 856×907 视口下，原 dialog 为 fixed、inset:0、margin:0，边界 x=0/y=0，导致桌面弹窗贴左上角。显式设置 margin:auto 恢复桌面居中，保留 600px 以下 margin:auto 0 0 的底部弹层。滚动移入内部容器，使圆角边界完整裁切滚动条。

此次检查 1200、856、601、600、397、320px：桌面中心偏差均为 0，手机底边偏差均为 0；列表可滚动、无横向溢出。复测搜索、Esc 关闭与焦点返回。类型及 CSS 解析通过，截图 /tmp/lilink-responsive-dialog-qa/。

按用户长期要求，将「每次 UI 优化必须同时实测桌面和移动端，包含相关弹窗展开态及断点边界」写入原站与视觉副本的 Web AGENTS.md。上次桌面弹窗验证遗漏由本次修正，今后不能以单端检查代替双端验收。


后续反馈：用户取消飞鸟点缀。仅移除注册进度上方的飞鸟及配套虚线样式，保留桌面弹窗居中修复、手机底部弹层和双端验收规则。


## 2026-09-10 首页数字字体

- 倒计时和平台统计数字改用现有无衬线正文字体栈，保留等宽数字设置。
- 视觉预览模式下「已送出匹配」显示示例值 20。
- 副本 TypeScript 检查及两处 CSS 的 PostCSS 解析通过；仓库 CSS 脚本不覆盖副本目录，已用直接解析补充。
- 手机与 1440px 桌面浏览器已核对，数字无溢出；本次截图：`/tmp/lilink-ui-fonts/mobile.png`、`/tmp/lilink-ui-fonts/desktop.png`。


### 数字字体修订：Playfair Display

根据用户 CampusDate 参考图，核对本地 LandingView 的数字 serif 类及全局字体规则，改用同款 Playfair Display 700；字体文件从本地参考资源复制，仅应用于首页倒计时和统计数字。示例值 20 保留。CSS 解析通过，405px 手机与 1440px 桌面已目视确认无溢出；截图位于 `/tmp/lilink-ui-fonts/playfair-mobile.png` 和 `/tmp/lilink-ui-fonts/playfair-desktop.png`。


## 2026-09-10 个人资料信息层级

合并名片与关于你，首页按本轮参与/已揭晓匹配/资料与服务分组，移除预览中的见面流程。方案、实现范围和验证见 [个人资料层级与见面流程退出](07-个人资料层级与见面流程退出.md)。


## 2026-09-10 生图方案落地：我的资料

- 用户要求移除「我的」里的重复资料入口，已移除，底部导航保留。
- 使用 imagegen 生成资料页视觉稿，保存为 `设计参考/我的资料-生图设计稿.png`，随后用真实表单实现。
- 标题与保存状态同排；分组采用下划线导航；移除资料外层卡片，采用自我介绍/联系方式/基本信息直接分节。昵称带首字标记，介绍展示真实字符数，日期选择压缩为三列。
- 保留原有自动保存、错误提示与重试，待保存/保存中分别显示；联系方式仍独立展开编辑。
- 副本 TypeScript、针对改动的 ESLint（复用 apps/web 配置）及 PostCSS 解析通过。375px、548px 手机和 1440px 桌面检查完成，窄屏无横向溢出；分组切换和联系方式展开正常。「我的」已核对不再包含重复资料入口。
- 实际截图保存在 `/tmp/lilink-profile-imagegen/`。视觉稿仅用于设计参考，页面未以截图替代交互。


## 2026-09-10 资料页层级简化与联系方式可见性

去掉顶部口号、分组导航底线及问题间分割线，以标题与间距分区。联系方式独立标题下直接显示首选渠道和已保存的具体内容，编辑入口采用强调底色；未保存的输入不替换摘要。手机 481px 和桌面 1440px 已检查，展开编辑、空值校验、保存反馈通过。TypeScript、ESLint 与 CSS 解析通过。截图：`/tmp/lilink-profile-clear/`。


## 2026-09-10 参考 CampusDate 的问卷题卡与保存反馈

- 已检查本地 `/sjtu/survey` 实际渲染及 `campusdate/assets/SurveyView-sQWpnQwe.css` 的 `.question-card`、`.survey-nav` 等规则，采用白底、轻边框、圆角独立题卡与明确的模块按钮，保留 LiLink 配色。
- 顶部说明基本信息、匹配偏好与价值观是算法匹配依据；明确问卷自动保存，联系方式独立保存。
- 模块按钮增加序号、按钮底色和选中态，模块末尾提供上一/下一模块。切换后滚动并聚焦新模块标题。
- 保存状态随滚动保持可见，模块末尾重复展示：未填写、待保存、保存中、完整资料已保存、草稿已保存、保存失败与重试。当前输入与已保存快照不一致时不显示保存成功。
- 最后一模块只有完整资料已保存后才能点击「填写完成，返回首页」。切换模块不会重建问卷编辑器，保留已有自动保存队列。
- 浏览器实走三个模块前进/返回与最终返回首页；昵称编辑观察到待保存 → 全部修改已保存，随后恢复示例昵称。375px 与 1440px 题卡检查完成，窄屏无横向溢出，截图 `/tmp/lilink-survey-cards/`。
- TypeScript、ESLint、CSS 解析通过；新增保存状态测试 4 项通过，覆盖旧资料存在时的新修改、保存中、失败、草稿及空白状态。失败/草稿分支为单元测试验证，浏览器保存使用虚拟预览响应，不代表真实后端持久化验收。
- 本次实施问卷布局与导航/保存提示，先前记录的冗余字段决策仍见 `08-匹配偏好冗余选项决策.md`，未在本次修改匹配算法或字段契约。


## 2026-09-10 题卡与底部再次微调

按用户要求将分组按钮回退为文字加选中下划线，移除模块序号。底部删除重复保存说明，居中排列轻量上一模块链接和无阴影的下一步/完成填写按钮；保留顶部保存状态及原完成条件。题卡桌面圆角 32px、手机 28px，增加内部留白。375px 与 1440px 底部截图已核对，前后模块切换通过；TypeScript、ESLint 和 CSS 解析通过。证据：`/tmp/lilink-survey-refine/`。


## 2026-09-10 模块导航对齐参考

顶部三模块包装为轻卡片按钮，中间箭头连接；删除重复模块标题和部分序号。切换后的滚动及焦点移至模块导航。底部按 CampusDate 参考采用居中浅色胶囊按钮、中央 1 / 3 页码，首模块禁用上一模块，末模块保留保存完成条件。手机和桌面已核对，连续下一模块切换通过，TypeScript/ESLint/CSS 检查通过。截图 `/tmp/lilink-module-navigation/`。


## 2026-09-10 表单展示与完成反馈

按 CampusDate 公开 CSS 的 dual-select 上下两行结构调整对方身高/体重范围；保留字段契约。昵称与介绍合并至柔和圆角卡片。邮箱、微信、QQ、电话直接展示，单选决定交换后展示哪一种，保留独立保存和空值校验。完成填写仅在当前完整快照已保存时显示下方成功反馈，不再跳转；修改内容后旧确认不再显示。手机、桌面截图和范围交互已核对；验证空首选联系方式拦截、邮箱保存成功、完成后留在原页面。TypeScript/ESLint/CSS 检查及 4 项保存状态测试通过。截图 `/tmp/lilink-profile-fields/`；保存验证使用本地虚拟数据。


## 2026-09-10 仅关于你采用 A 方案

仅 self 模块新增 selfFlat 作用域：移除自我介绍、联系方式和基本信息题卡的背景/边框/阴影与外层内边距，保留输入控件边界及所有字段和保存行为；移除昵称输入中的重复首字头像。其他两个模块的题卡和逻辑不变。手机及桌面预览截图保存于 `/tmp/lilink-about-flat/`。TypeScript、ESLint 与 CSS 解析通过。


## 2026-09-10 关于你信息层级与联系方式自动保存

按用户截图：四种联系方式横向单选，仅展示选中渠道输入框，删除手动保存按钮；基本信息改为标签在左、控件在右，性别使用原生单选圆圈，身高紧随其后。所有修改限定于关于你，其他模块布局保留。联系方式停止输入 500ms 后自动保存，串行请求避免旧请求晚到覆盖新值；空白选中渠道不提交并提示补全，失败可重试。模块切换仅隐藏关于你，不卸载联系方式编辑器，保留编辑及待保存请求。顶层保存反馈与完成按钮也考虑联系方式未保存状态。

已用虚拟预览验证空渠道提示、微信输入自动保存、跨 tab 保留与保存完成，恢复邮箱示例。375px 和 1440px 已检查，截图 `/tmp/lilink-contact-inline/`。TypeScript、ESLint 与 CSS 解析通过。过程中出现 Next CSS 热更新 removeChild 错误，已刷新预览；保存验证不代表真实后端持久化验收。


## 2026-09-10 标签与模块按钮微调

隐藏日期选择上方重复年月日标签，保留控件可访问名称；语言与（可多选）固定两行，附注字号降低；三个模块按钮圆角改为胶囊形。手机、桌面已核对，TypeScript 与 CSS 解析通过。截图 `/tmp/lilink-profile-labels/`。


语言标签按最新批注改为单行「语言（多选）」，附注字号 0.65rem，标签列按内容宽度避免拥挤。手机及桌面已核对，CSS 解析通过；截图 `/tmp/lilink-language-inline/`。


## 2026-09-10 未完成问卷定位

参照 CampusDate SurveyView 最后模块的条件按钮：本副本最后模块未填完时显示「还有 N 题未完成，点击定位」，点击切换模块并滚动、聚焦和短暂高亮首道未完成题；全部填完后显示「完成问卷」，仍需保存完成才可点击，成功反馈留在原页。计数基于当前输入与必答标记，同题范围上下限合并计数，不含选填项。已验证清空昵称的跨模块定位、价值观多选清空后的计数与同模块定位、恢复答案后的完成成功。手机与桌面截图 `/tmp/lilink-incomplete-jump/`。TypeScript、ESLint、CSS 解析通过，验证使用虚拟预览数据。


## 2026-09-10 关于你恢复大分区卡片

按最新批注给自我介绍、联系方式、基本信息各增加一层统一圆角白色外卡片，内部保留无独立题卡的平铺字段。其他模块与保存行为不变。375px 和桌面已核对，TypeScript/CSS 解析通过；截图 `/tmp/lilink-section-surfaces/`。


## 2026-09-10 统一桌面页眉导航

移除 AppShell 桌面侧栏，将首页/我的匹配/我的资料/我的放入顶部横向导航，右侧保留账号菜单；内容居中使用完整宽度。桌面聚焦子页同样提供统一导航，手机保持原底部导航及子页返回标题。935px 桌面、375px 手机和 879/880px 断点检查通过，账号菜单和顶部跳转正常。TypeScript、ESLint、CSS 解析通过；截图 `/tmp/lilink-unified-header/`。仅修改设计副本。


## 2026-09-10 匹配页送信前置动画

- 读取 CampusDate MatchView 公开 CSS/JS：封口 rotateX(-180deg)，0.8s 翻盖、约 1s 切换、0.5s 结果淡入。以其开封顺序为参考，用本项目鸽子衔信 SVG 与原生 CSS 实现网页交互动画。
- 本次流程：鸽子衔信飞入 → 信封落定 → 用户点击打开 → 封口翻开、信纸抽出并展开 → 匹配结果淡入。送达约 2.2s，打开约 1.8s。每次进入页面播放，点击跳过可直接查看。
- 仅已有非受限匹配且有对方数据时启用。无匹配/受限状态直接显示原业务状态；开封不调用引荐、举报或其他写接口。
- 支持原生键盘按钮和减少动态效果，计时器随卸载清理；结果展示后焦点转入结果区域。保留已恢复历史入口与并列举报按钮。
- 我的资料标题及说明改用匹配页同一组排版类。
- 实测手机和桌面的送信、翻盖/抽纸连续截图、结果终态及跳过；结果中历史入口与举报按钮可见。TypeScript、ESLint、CSS 解析通过。减少动态效果及无匹配分支为代码检查，尚未实测偏好切换。截图 `/tmp/lilink-match-reveal/`。此为设计副本网页动画，不是导出视频。


## 2026-09-10 首次揭晓与动态鸽子

- 取消独立打开和跳过按钮，信封本身支持点击/键盘开封。
- 按用户及匹配所属轮次记录已揭晓（无轮次信息时退回唯一匹配 ID，避免误用当前报名轮次）。完成开信后记录，本轮再次进入直接显示结果且不播放结果淡入；中途离开尚未完成则保留再次开信机会。
- 当前记录使用 localStorage，只保证同浏览器持久化；存储不可用时退回进程内记录。正式跨设备行为仍需后端支持。
- 鸽子不再引用静态 Logo 图片，改为独立 SVG 身体、远翼、近翼、尾羽、喙部和信件；双翼分相位摆动，身体起伏，尾羽与信件随动。飞行和落信仍采用原生 CSS 动画，保留减少动态效果支持。
- 浏览器记录振翅连续帧，验证信封开封及结果后刷新：揭晓区域数为 0、匹配标题可见；两个独立按钮不再存在。新增 2 项测试覆盖用户/轮次隔离与存储不可用。TypeScript、ESLint、CSS 解析通过。连续帧 `/tmp/lilink-dove-motion/`。


鸽子动画按用户最新要求回退为原 Logo SVG 整体飞入与轻微起伏，移除分层振翅绘制；保留每轮首次揭晓记录、点击信封开封及已移除的文字按钮状态。未重置用户已看记录，本轮不强制重播。


## 2026-09-10 找回密码视觉对齐

第一步复用登录页圆角卡片、居中标题和胶囊按钮；第二步复用注册流程的进度条、轻量表单和圆角控件，移除旧装饰眉题与大块发送状态底色。说明改用注册邮箱，虚拟预览空邮箱显示示例地址。验证码及密码重置逻辑保持原样，未发送真实验证码或修改凭据。手机、桌面两步和返回邮箱入口已验证。TypeScript/CSS 检查通过，截图 `/tmp/lilink-password-alignment/`。


找回密码第一步合并邮箱与验证码，发送/重发按钮置于验证码旁，下一步仅收集新密码。验证码仍在最终重置接口核验，未新增虚假验证步骤。修改邮箱清除验证码与发送状态，发送期间禁止编辑邮箱。手机/桌面预览及步骤往返已检查，TypeScript 通过；未发送真实验证码或修改密码。截图 `/tmp/lilink-reset-combined/`。


注册账号设置步骤：学校邮箱和普通邮箱两条流程均移除已验证邮箱摘要/修改邮箱；协议复选框和文本固定横排，保留协议链接及勾选逻辑。手机和桌面两种注册页已核对，375px 仍为一行。TypeScript 与 CSS 解析通过。截图 `/tmp/lilink-register-clean/`。未创建账号或代用户同意协议。


普通邮箱注册学校选择箭头改为 16px 酒红线性箭头，距右侧 16px，文字预留 44px 空间；保留原生 select 交互。手机与桌面核对，CSS 解析通过。

## 2026-09-11：首页活动入口与参与引导落地

- 用户确认首页设计稿后，替换视觉副本 `/dashboard` 的首页主区域，保留原导航、匹配结果、资料与服务入口及报名 API。
- 使用 `HomeOverview.tsx` 与独立 CSS Module：桌面左侧本轮报名、右侧活动；手机活动短横幅居前，三步教程纵向展示。
- 文案改为“你已报名本轮匹配”，说明匹配意向、无需重复报名、北京时间的结果公布时间，以及“公布后前往我的匹配查看结果”。报名与取消成功提示同步改为直白表述。
- 原有意向选择、锁定判断、保存错误和加载状态继续沿用。取消报名增加确认弹窗。
- 1v1 服务介绍已可打开；因没有真实登记入口，明确显示活动筹备中、暂未开放登记，不模拟已提交。
- 验证：副本 TypeScript 检查通过；agenda-redesign 的 2 项测试通过；CSS 使用项目 PostCSS 校验函数通过（根命令默认跳过 docs 副本）。浏览器检查桌面 1280px、手机 390px、教学及活动弹窗；在虚拟预览中验证取消报名后重新报名成功。
- 截图位于 `design-explorations/home-integrated-desktop.png`、`home-integrated-mobile.png`、`home-integrated-mobile-guide.png`。未部署。

### 首页独立状态预览与倒计时

- 首页顶部增加独立的“首页状态预览”：当前状态、首次加入、待报名、已报名、报名已锁定、匹配已公布、本轮未匹配、暂无开放轮次。仅在视觉预览模式显示；演示报名和取消只更新组件内状态，不请求报名 API。
- 恢复原 RevealCountdown 的天／时／分／秒计时，保留公布日期与北京时间说明；结果演示及暂无轮次不显示倒计时。
- 副本 typecheck、CSS 解析通过；390px 与 1280px 检查七种演示状态，无横向溢出。实测待报名按钮切换到已报名，观察秒数持续变化。截图 `design-explorations/home-states-390.png`、`home-states-1280.png`。

- 用户追加确认：首页删除倒计时下方的“公布时间（北京时间）”与具体日期，仅保留倒计时和结果查看提示。

- 用户追加确认：首页报名卡移除“本轮匹配 · 秋日来信 / 免费参与”整行及分隔线，收紧顶部留白。

### 意向弹窗、教学跳转与注销入口

- 意向选择改成居中弹窗，移除底部拖动条和滑入动画；通过组件独立样式限定范围，不改变邀请分享弹层。说明改为中文操作提示。
- 三步教程右侧增加资料页链接、报名/更换意向操作、匹配页链接；资料未完成和报名关闭时不提供无效报名操作。
- 我的页面在修改密码下增加注销账号，点击打开说明弹窗。仅视觉入口，未接入删除 API，未删除任何账号。
- 副本类型检查及 CSS 解析通过；手机检查居中意向弹窗，实际点击资料链接到达资料页；桌面与手机检查注销入口及说明弹窗。截图保存于 design-explorations/intent-centered-mobile.png 和 delete-account-preview-mobile.png。

### 首页取消下方待办列表

- 用户确认：首页不再显示下方 todo list。移除“已揭晓的匹配”和“资料与服务”两个列表；教程保留。
- 匹配状态的标题、说明与原操作入口合并到上方主卡片，包括已交换联系方式状态；当前状态与匹配已公布预览复用已有匹配内容。
- 我的页面将修改密码、注销账号分成两个独立卡片。
- 副本类型检查通过；桌面、手机确认主卡片显示匹配进展且下方无待办列表，检查个人中心卡片分离。未修改 API 或删除数据。

### 活动入口改为轮播

- 活动卡支持前后切换、分页指示与触摸左右切换；默认手动切换。
- 保留 1v1，视觉预览增加“商家合作 · 活动示意”卡。无真实商家、优惠或报名承诺；正式模式不展示该示例。
- 类型检查通过，浏览器验证点击切换及手机、桌面布局。

- 轮播追加调整：箭头、圆点与暂停按钮统一尺寸及中心线，整排居中，移除分散的页码。每 5 秒自动切换；悬停、焦点位于轮播内、活动弹窗打开时暂停，提供显式播放/暂停按钮，页面隐藏时不切换。浏览器确认自动切换及暂停按钮状态，检查手机与桌面；typecheck 与 CSS 解析通过。

- 轮播控件按用户最终要求简化：移除前后箭头、播放/暂停按钮，只保留居中等大圆点，以深浅标识当前项，点击直接切换。保留 5 秒自动轮播及悬停暂停。类型检查通过，实测圆点点击切换。

- 轮播指示器最终确认：当前项为短横线，其余项为圆点；保留点击切换、自动播放及悬停暂停。

- 活动轮播改为横向轨道，点击分页或自动切换时使用 420ms 平移过渡，图片与文案一起滑动；保留当前项短横线。非当前活动禁止聚焦；减少动态效果偏好下关闭过渡。类型检查通过，浏览器确认轨道的 transform 过渡样式及手机、桌面布局。

- 教程入口按用户要求只在当前阶段显示；已完成和未到达阶段不展示右侧操作。浏览器切换首次加入、待报名、匹配已公布，确认三行操作数分别为 1/0/0、0/1/0、0/0/1。类型检查通过。

### 账号入口收进头像菜单

- 移除“我的”主导航，底部改为首页、我的匹配、我的资料三等分。
- 头像菜单“关于平台”“常见问题”替换为我的邀请、我的优惠券，新增账号设置。
- 新增 `/dashboard/settings`，仅保留账号信息、修改密码、注销账号；旧 `/dashboard/me` 跳转账号设置。
- 注销警告按用户要求说明永久删除、无法恢复，原邮箱可重新注册新账号但不恢复原记录。仍为 UI 预览，未接入真实注销及邮箱释放逻辑。
- 副本类型检查通过；手机验证三项导航、头像菜单、账号设置和注销警告。

- 用户最终调整：移除账号设置中间页及菜单项，头像菜单直接提供修改密码、注销账号。旧 settings/me 地址回到首页。注销警告移到 AppShell，保留永久删除与原邮箱可重新注册说明，仍不执行真实注销。类型检查通过，浏览器确认菜单及直接打开警告。

- 注销弹窗：永久删除警告改为红色，移除视觉预览说明文字；仅调整呈现，注销执行逻辑未变化。

- 匹配标题中的对方昵称单独使用楷体字体栈（Kaiti SC / STKaiti / KaiTi），莓红色、略放大，与周围标题区分。使用实际对方昵称定位，不写死示例姓名。类型检查通过，手机预览确认。

- 桌面首页主卡片内容整体垂直居中，保持左对齐；仅应用于 761px 以上，手机布局不变。CSS 解析及桌面、手机浏览器检查通过。

- 报名锁定状态：移除主卡片“查看参与说明”及操作区留白；提示改为“已进入匹配锁定期，无法更改匹配意向或取消报名”。浏览器确认锁定状态无该按钮，类型检查通过。

- 教程标题与序号光学校准：标题行高固定 1.4，并上移 1.5px，纠正中文标题视觉偏下；手机浏览器确认三行对齐。

- 主卡片不再提供“查看参与说明”兜底入口；无操作时不渲染操作区，暂无开放轮次状态同步移除多余留白。教程区说明入口保留。

- 基本信息窄屏对齐：600px 以下统一 64px 标签列和 8px 间距，出生日期标题与三个日期控件同排，颜值自评三个选项同排，语言控件与上下控件共用起点。收紧日期显示层内边距以避免年月日截断。CSS 解析通过，手机和桌面浏览器确认。

- 出生日期改为单一日期入口，点击居中日历弹窗，可选年份、月份和具体日期。沿用原年份范围与自动保存链路。浏览器验证选择 2003-05-12 回填、重新打开高亮日期，并检查手机和桌面弹窗；类型检查通过。

- 参与说明弹窗重排为编号圆点、步骤标题、短说明三层结构；强调每轮需要报名、资料不会自动报名。底部规则单独分隔，确认按钮全宽。手机浏览器检查及类型检查通过。

### 匹配成功后自动邮件通知（产品方向）

- 用户提出：参加本轮并匹配成功后，在结果公布时自动向双方发送邮件。首页教程、说明弹窗及等待结果提示同步采用该文案。
- 本次仅更新视觉副本，未实现或验证邮件发送。邮件内容是否包含联系方式、是否取消双方确认机制尚未确定；本次不修改联系方式权限、匹配接口及引荐逻辑。

### 匹配页采用 B 版来信布局

- 顶部“本周来信”与“过往匹配记录”并排，保留原品牌与三项导航；内容按手写名字、学校、自我介绍、基本信息、联系方式呈现。使用现有资料，不将生图中扩写的自我介绍当真实内容。
- 增加复制联系方式及成功/失败提示，长联系方式自动换行；已引荐状态不再显示无效按钮。
- 举报本次匹配移到信件卡片之外的页面底部，独立于联系方式，继续使用原举报表单。未更改联系方式展示权限、引荐与举报 API。
- 副本类型检查、CSS 解析通过。手机、桌面检查；实测复制提示“已复制”、打开举报表单后取消，过往记录入口到达历史页。截图：design-explorations/match-letter-mobile.png、match-letter-desktop.png。

### B 版按参考图再次复刻

- 补齐顶部信封分隔、大号手写姓名与斜线、右上角 A letter to you、两列图标资料、信件底部贴边联系方式区域与枝叶装饰；移除参考图没有的独立匹配度区，学校移入两列资料。举报继续独立放在信件外侧底部。
- 用户明确新规则：参与本轮且匹配成功后直接展示联系方式，不再要求交换。本次副本移除 introducedAt 展示门槛和交换操作按钮，使用接口已提供的联系方式；保留 LIMITED 状态隐藏。真实后端联系方式返回及邮件发送尚未实现/验证。
- 示例资料换成自然的较长介绍，不再显示排版测试文案。类型与 CSS 检查通过，375px 检查及复制联系方式成功；截图 match-letter-b-replica-mobile.png。


### 来信页花字与联系方式精修

- 对方名字使用本地 WOFF2 字体 Liu Jian Mao Cao（刘建毛草），原始文件来自 Google Fonts 官方仓库，保留完整字库及 OFL 许可；不再依赖系统楷体。仅匹配页名字应用花字，保留真实文本。
- 联系方式改为图标 / 标签 / 账号三列共用一条顶线，按钮与图标左缘对齐，右侧为细线枝叶。微信图标使用 Simple Icons 的双气泡图形，沿用目录中的 CC0 许可。
- 示例微信号按用户要求缩短为 chenyinuo_29，长真实联系方式仍允许自然换行，未截断数据。
- 卡片收紧至约 167px（375/446px 预览短账号场景），已复制提示与按钮中心重合；状态行预留高度避免复制后跳动。
- 过往匹配记录按钮加宽：桌面至少 154px，常规手机 142px，380px 以下 132px。
- 预览使用“联系方式”标题，保留此前直接展示联系方式的产品决定；举报仍在信件外侧底部。
- 验证：副本 typecheck、CSS 解析通过；浏览器检查 375px、446px 与 1280px，375px 联系框高度 166.5px，无横向溢出；控件左缘一致，复制后的“已复制”文本居中。最终截图为 design-explorations/match-contact-polish-375.png、match-contact-polish-446.png、match-contact-polish-desktop.png。


### 字体、举报弹窗与预览报名修正

- 名字由难辨认的刘建毛草换为笔画完整的龙藏体，保留本地字库与字体许可。
- 举报弹窗缩小宽度，按可用视口计算左右各 20px 留白（窄屏）；正文标点使用严格换行规则。
- 首页预览报名不再直接跳到已报名：主按钮和教程入口均先打开 IntentSheet，取消保持待报名，选择意向后更新预览状态及对应意向；不调用真实报名接口。
- 类型检查及 CSS 解析通过；实测主按钮、取消、教程入口、选择约会、重新打开回显；检查 375/427px 手机和 1280px 桌面。
- 用户最新确认：信封两侧分隔线恢复原来的直线，撤回弧线。其他三项修正保留。

- 头像菜单移除“返回首页”项，其余入口不变。

### 等待揭晓与未匹配卡片统一为来信样式

- 重构 MatchWaitingStrip：暖白信纸背景、9px 圆角、细暖色边框、衬线标题；移除原有渐变、小圆钟图标及浮起阴影。
- 两种状态共用淡粉色底部操作区，按钮采用来信页莓红与描边样式；保留原资料、历史入口和状态判断。
- 等待卡片按天/时/分/秒显示倒计时，保留揭晓时间；说明同步既定匹配成功邮件通知的产品文案，未增加邮件实现。
- 未匹配说明改为暂未找到合适同学，并提示检查资料、下轮报名；删除没有具体证据的原因推断与提升成功率保证。
- 新增 WaitingLetter、UnmatchedLetter Storybook 示例，继承 smoke 标签。副本类型检查与 CSS 解析通过。
- 浏览器验证手机和桌面的等待、未匹配两种状态，375px 无横向溢出；资料入口与历史入口实际导航成功。证据：design-explorations/waiting-letter-mobile.png、waiting-letter-desktop.png、unmatched-letter-mobile.png、unmatched-letter-desktop.png。

- 根据“太方了”的反馈，匹配成功、等待揭晓、未匹配三类卡片外层统一改为 24px 圆角，弱化边框并增加极轻阴影；联系方式区上角 20px、下角 24px，与外层衔接。CSS 解析通过，浏览器检查三种手机状态及桌面来信卡片。

- 等待揭晓、未匹配卡片取消底部操作区的粉色背景和顶部分隔线，按钮直接融入卡片；未匹配卡片移除“查看历史”，顶部“过往匹配记录”保留。类型检查通过，手机检查两种状态。

### 过往匹配页面视觉统一

- 移除本周来信标题下的信封分隔与标语，移除等待卡片倒计时上方分隔线。
- 历史页移除渐变大页头与重复“过往匹配”标题，改为返回入口、标题与轮数、简短说明；空状态使用柔和圆角卡片和返回本轮匹配按钮。
- 有记录时按轮次独立展示24px圆角卡片，突出轮次、日期和匹配结果状态；详情、举报及受限展示判断沿用原逻辑。
- 类型检查、CSS解析通过；浏览器检查空记录及未匹配记录的手机/桌面效果。截图 history-empty-desktop.png、history-record-mobile.png 位于 design-explorations。

- 历史页增加三种卡片示例：未匹配到、匹配成功、未参与匹配。仅视觉模式默认显示虚拟三轮，顶部可切换当前数据与空状态；正式数据不变。
- 成功卡片摘要突出姓名、学校与匹配度，详情可展开；其他两种卡片保持简洁说明，用不同低饱和状态标签区分。
- 历史详情沿用已确认的直接显示联系方式口径，移除引荐按钮/状态，仍保留 LIMITED 隐藏和举报机制。
- 类型与CSS检查通过，手机查看展开详情及桌面三卡片。截图 history-three-expanded-mobile.png、history-three-desktop.png。

- 历史页口径更正为“最近三轮”，页头与空状态说明同步；前端最多展示三轮，不删除历史数据。
