# 匹配历史与举报入口决策

## 最新决定

用户撤回取消历史匹配的决定：保留查看过往匹配记录，将入口放在当前匹配卡片下方。设计副本已恢复历史页。举报仍与已引荐并列，位于右侧。

以下为已被最新决定覆盖的历史记录。

# 匹配历史退出与举报入口决策

2026-09-10，用户明确要求记录。状态：设计副本页面已实施，正式站与后端权限待实施。

## 用户端历史匹配

取消查看过往匹配的功能，移除「查看过往匹配记录（N轮）」入口。后续实施需覆盖历史页面及用户端读取权限，不能只隐藏入口。

该决策不等于删除历史数据库记录，也不等于取消运营审计；本轮未授权数据删除。

## 举报入口

「举报本次匹配」移入匹配卡片，与「已引荐」同一行并列，放在右侧。不再作为卡片下方的独立按钮。

## 当前边界

本轮仅保存产品决策，未修改设计副本、原站页面、后端或线上服务。


## 页面实施

设计副本已移除历史入口，两个旧历史路由重定向当前匹配页；举报操作移入匹配卡片右侧，与已引荐/交换联系方式并列，保留已有举报状态限制。375px、1440px 页面和举报弹窗打开/取消已检查；旧历史地址确认回到当前匹配。TypeScript 和 CSS 解析通过，ESLint 发现的多余 Link import 已移除。截图 `/tmp/lilink-match-actions/`。未更改正式站或后端读取权限，不代表生产历史访问已下线。
